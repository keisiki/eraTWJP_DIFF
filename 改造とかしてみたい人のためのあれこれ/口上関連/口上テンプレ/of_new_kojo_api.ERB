;これらの関数は口上別に設定できるが、custom_button_name、custom_talentといった変数はキャラクター変数のため
;複数口上がある場合、IDを被らせると上書きされてしまうので注意。

;if the scuuess corresponding to com not exist, it will be called
;you can use it to do some data process before com
;@M_KOJO_XXX_SUCCESS_KX_GENERAL

;general version of EXTRASOURCE
;EXTRASOURCEの汎用版
@M_KOJO_XXX_EXTRASOURCE_SCOM_KX_GENERAL
@M_KOJO_XXX_EXTRASOURCE_COM_KX_GENERAL

;================================
;custom shop button
;カスタムSHOPボタン
;y is the id of the button, range from 0 to 9
;yはボタンのID、0~9に対応
;================================
;judge the button can be shown, if return 1, it will show
;これをRETURN 1するとボタンが表示される
@KOJO_XXX_CUSTOM_BUTTON_CONDITION_KX_Y
custom_button_name:XX:Y = ボタンY
RETURN 1

;when the button is clicked
;ボタンがクリックされた場合
@KOJO_XXX_CUSTOM_BUTTON_KX_Y

;================================
;custom talent(copy form ckr)
;カスタム素質
;Z is the id of the talent, range from 0 to 29
;Zは素質ID、0~29に対応
;if this variable != 0, it will show
;この変数が0でなければ表示

;custom_talent:XX:Z = 1

;the name of the talent
;素質名
;custom_talent_name:XX:Z = カスタム素質

;the color of the talent(0 is GETDEFCOLOR())
;カスタム素質の色
custom_talent_color:XX:Z =
;================================
;custom com
;y is the id of the button, range from 0 to 29
;カスタムコマンド、0~29まで設定可能
;================================
;コマンド名
@KOJO_XXX_COM_NAME_KX_Y
	RESULTS = com_name

;judge
;judge the button can be shown, if return 1, it will show
;コマンド使用条件、通常のCOM_ABLE関数と同じくRETURN 1すれば実行可能としてコマンドが表示される
@KOJO_XXX_COM_ABLE_KX_Y
	RETURN 1

;================================
;実行可能判定に係る部分、通常は書かなくてよい
@KOJO_XXX_COM_KX_Y
;refer to @CAN_COM9(ARG)
;or you can don't write it
@KOJO_XXX_CAN_COM_KX_Y(skipFlag)
	#DIM skipFlag
	SKIPDISP skipFlag
	SKIPDISP 0
;================================

;================================
;auto update when load game
;ゲームロード時に読み込まれるオートアプデ
;if your kojo can't keep continuouing updates, ignore it
;口上更新が続けられなくなったら無視してよい
;;================================
@KOJO_XXX_VERSION_KX
	;現在の口上のバージョン数をRETURNする
	; RETURN now_version

;when the CFLAG:kojo_version in save small than KOJO_XXX_VERSION_KX, it will be called
;セーブデータのCFLAG:kojo_versionがKOJO_XXX_VERSION_KXより小さい場合読み込まれる
@KOJO_XXX_VERSION_UPDATE_KX
	;
	; RETURN now_version
