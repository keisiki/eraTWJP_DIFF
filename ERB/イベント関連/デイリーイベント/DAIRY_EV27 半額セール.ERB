@DAILY_CONDITION_EV27

IF !FLAG:土産屋大安売り && !RAND:30
	CALL DAILY_EV27
	RETURN 1
ELSE
	RETURN -1
ENDIF

@DAILY_EV27
;この後にフラグリセットが入るので目印として+1100しておく
FLAG:土産屋大安売り = RAND:11 + 1100
SIF FLAG:土産屋大安売り % 100 == MAIN_MAP
	RESTART
IF FLAG:新聞購読
	PRINTFORML 文文。新聞によると、%STR:(5000 + (FLAG:土産屋大安売り % 100) )%方面の土産屋が大安売りを始めたらしい
	PRINTFORMW 全品半額セールのようだ
ENDIF
RETURN 1