
@MESSAGE_脱衣上着;だけ
IF SHOW_TOP(TARGET) != ""
	IF TEQUIP:3 == 0; && EQUIP:TARGET:上半身下着１
		PRINTFORM %CALLNAME:TARGET%\@ FLAG:70 ? の # が \@
		IF EQUIP:TARGET:上半身上着１
			IF EQUIP:TARGET:ワンピース
				PRINTFORM %CLOTHNAME(17, EQUIP:TARGET:上半身上着１)%
				SIF EQUIP:TARGET:上半身上着２
					PRINTFORM と%CLOTHNAME(16, EQUIP:TARGET:上半身上着２)%
				SIF EQUIP:TARGET:着物
					PRINTFORM と%CLOTHNAME(14, EQUIP:TARGET:着物)%
				PRINTFORM と%CLOTHNAME(13, EQUIP:TARGET:ワンピース)%を脱\@ FLAG:70 ? がせる # ぐ \@と、
			ELSE
				PRINTFORM %CLOTHNAME(17, EQUIP:TARGET:上半身上着１)%
				IF EQUIP:TARGET:上半身上着２
					PRINTFORM を脱\@ FLAG:70 ? がせ # ぎ \@%CLOTHNAME(16, EQUIP:TARGET:上半身上着２)%
					SIF EQUIP:TARGET:着物
						PRINTFORM と%CLOTHNAME(14, EQUIP:TARGET:着物)%
					PRINTFORM を肌蹴\@ FLAG:70 ? させる # る \@と、
				ELSEIF EQUIP:TARGET:着物
					PRINTFORM を脱\@ FLAG:70 ? がせ # ぎ \@%CLOTHNAME(14, EQUIP:TARGET:着物)%を肌蹴\@ FLAG:70 ? させる # る \@と、
				ELSE
					PRINTFORM を脱\@ FLAG:70 ? がせる # ぐ \@と、
				ENDIF
			ENDIF
		ELSEIF EQUIP:TARGET:上半身上着２
			PRINTFORM %CLOTHNAME(16, EQUIP:TARGET:上半身上着２)%
			IF EQUIP:TARGET:ワンピース
				SIF EQUIP:TARGET:着物
					PRINTFORM と%CLOTHNAME(14, EQUIP:TARGET:着物)%
				PRINTFORM を肌蹴させ%CLOTHNAME(13, EQUIP:TARGET:ワンピース)%を脱\@ FLAG:70 ? がせる # ぐ \@と、
			ELSE
				SIF EQUIP:TARGET:着物
					PRINTFORM と%CLOTHNAME(14, EQUIP:TARGET:着物)%
				PRINTFORM を肌蹴\@ FLAG:70 ? させる # る \@と
			ENDIF
		ELSEIF EQUIP:TARGET:着物
			PRINTFORM %CLOTHNAME(14, EQUIP:TARGET:着物)%
			IF EQUIP:TARGET:ワンピース
				PRINTFORM を肌蹴させ%CLOTHNAME(13, EQUIP:TARGET:ワンピース)%を脱\@ FLAG:70 ? がせる # ぐ \@と、
			ELSE
				PRINTFORM を肌蹴\@ FLAG:70 ? させる # る \@と、
			ENDIF
		ELSE ;ワンピのみ
			PRINTFORM %CLOTHNAME(13, EQUIP:TARGET:ワンピース)%を脱\@ FLAG:70 ? がせる # ぐ \@と、
		ENDIF

;		IF SHOW_TOP(TARGET,1) != ""
;			PRINTFORM %CALLNAME:TARGET%\@ FLAG:70 ? の # が \@%SHOW_TOP(TARGET,1)%を肌蹴\@ FLAG:70 ? させる # る \@と、
;		ELSE
;			PRINTFORM %CALLNAME:TARGET%\@ FLAG:70 ? の # が \@%SHOW_TOP(TARGET,2)%を脱\@ FLAG:70 ? がせる # ぐ \@と、
;		ENDIF
		
		IF SHOW_上半身下着(TARGET) != ""
			PRINTFORM %OPPAI_DESCRIPTION(TARGET)%
			IF EQUIP:TARGET:レオタード || EQUIP:TARGET:ボディースーツ || EQUIP:TARGET:ワンピース || EQUIP:TARGET:着物
				PRINTFORM を包んでいる%SHOW_上半身下着(TARGET)%
				IF SHOW_BOTTOM(TARGET) == ""
					PRINTFORM と%SLIT(TARGET)%
				ELSEIF !EQUIP:TARGET:スカート
					PRINTFORM と%SHOW_BOTTOM(TARGET, 14)%
				ENDIF
				PRINTFORMW が露わになった
			ELSEIF EQUIP:TARGET:上半身下着１
				PRINTFORMW を包み込む%SHOW_上半身下着(TARGET)%が露わになった
			ELSEIF EQUIP:TARGET:下半身下着２
				PRINTFORMW がはだけた%SHOW_TOP(TARGET,1)%から覗いている
			ENDIF
		ELSE
			PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%
			IF EQUIP:TARGET:レオタード || EQUIP:TARGET:ボディースーツ || EQUIP:TARGET:ワンピース || EQUIP:TARGET:着物
				IF SHOW_BOTTOM(TARGET) == ""
					PRINTFORM と%SLIT(TARGET)%
				ELSEIF !EQUIP:TARGET:スカート
					PRINTFORM と%SHOW_BOTTOM(TARGET, 14)%
				ENDIF
			ENDIF
			PRINTFORMW が露わになった
		ENDIF
	ENDIF
ELSE
	PRINTFORMW %CALLNAME:TARGET%の%OPPAI_DESCRIPTION(TARGET, 1)%が露わになっている
ENDIF

IF PALAM:TARGET:欲情 >= PALAMLV:3
	IF PALAM:TARGET:欲情 >= PALAMLV:6
		PRINTFORM その%NIPPLE_DESCRIPTION(TARGET)%は、
		IF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000 && ABL:Ｂ感覚 <= 5
			IF RAND:2 == 0
				PRINT まるで乳辱への期待に打ち震えるかのように
			ELSE
				PRINTFORM %CALLNAME:PLAYER%へ乳嬲りをせがむかの如く
			ENDIF
		ENDIF
		IF RAND:4 == 0
			PRINT 快感から厭らしく膨れて
		ELSEIF RAND:3 == 0
			PRINT ビンビンになって
		ELSEIF RAND:2 == 0
			PRINT 乳輪からこんもりと膨らんで
		ELSE
			PRINT 乳輪ごとぷっくりして
		ENDIF
		PRINTFORMW \@ EQUIP:TARGET:上半身下着１ ? %SHOW_上半身下着(TARGET)%の布地を押し上げて #  \@いる…
	ELSEIF PALAM:TARGET:欲情 >= PALAMLV:3
		PRINTFORM その%NIPPLE_DESCRIPTION(TARGET)%は、
		IF RAND:3 == 0
			PRINT ツンと上向い
		ELSEIF RAND:2 == 0
			PRINT すっかり硬くなっ
		ELSE
			PRINT 勃起し
		ENDIF
		PRINTFORMW て\@ EQUIP:TARGET:上半身下着１ ? 、%SHOW_上半身下着(TARGET)%の布地を押し上げて #  \@いる…
	ENDIF
ENDIF

SIF !((EQUIP:TARGET:レオタード || EQUIP:TARGET:ボディースーツ || EQUIP:TARGET:ワンピース || EQUIP:TARGET:着物) && !(EQUIP:TARGET:スカート || EQUIP:TARGET:ズボン))
	RETURN
IF (EQUIP:TARGET:下半身下着１ || EQUIP:TARGET:下半身下着２)
	IF PALAM:潤滑 >= PALAMLV:3
		PRINTFORMW %UNDER_SKIRT_DESCRIPTION(TARGET)%は愛液で染みが出来ている…
	ELSEIF PALAM:潤滑 >= PALAMLV:2
		PRINTFORMW %UNDER_SKIRT_DESCRIPTION(TARGET)%が愛液で湿っている…
	ENDIF
ENDIF

	
@MESSAGE_脱衣下下着
IF EQUIP:TARGET:下半身下着２
	PRINTFORM %CALLNAME:TARGET%\@ !FLAG:70 ? が # から\@%PANTSNAME(EQUIP:TARGET:下半身下着２, TARGET)%を
	IF PALAM:TARGET:潤滑 >= PALAMLV:4
		PRINTFORM \@ !FLAG:70 ? 脱ぐ # 脱がせる\@
		IF GET_INT(TARGET, "下半身下着_ずらし可能", EQUIP:TARGET:下半身下着２, "透ける(前)") && !RAND:2
			PRINTFORMW と、%CALLNAME:TARGET%の%SLIT(TARGET)%はグチャグチャにぬかるみきっており、%PANTSNAME(EQUIP:TARGET:下半身下着２, TARGET)%は愛液を吸い込んでずっしりと重くなっている…
		ELSE
			PRINTFORMW と、%SLIT(TARGET)%から溢れた愛液がダラダラと太ももを伝っている…
		ENDIF
	ELSEIF PALAM:TARGET:潤滑 >= PALAMLV:3
		PRINTFORM \@ !FLAG:70 ? 脱ぐ # 脱がせる\@
		IF GET_INT(TARGET, "下半身下着_ずらし可能", EQUIP:TARGET:下半身下着２, "透ける(前)") && !RAND:2
			PRINTFORMW と、%SLIT(TARGET)%と%PANTSNAME(EQUIP:TARGET:下半身下着２, TARGET)%の間で愛液が糸を引いた…
		ELSE
			PRINTFORMW と、%CALLNAME:TARGET%の%SLIT(TARGET)%ははっきりと濡れている…
		ENDIF
	ELSEIF PALAM:TARGET:潤滑 >= PALAMLV:2
		PRINTFORMW \@ !FLAG:70 ? 脱ぐ # 脱がせる\@と、%CALLNAME:TARGET%の%SLIT(TARGET)%はしっとりと湿っていた…
	ELSE
		PRINTFORMW \@ !FLAG:70 ? 脱いだ… # 脱がせた…\@
	ENDIF
ELSEIF EQUIP:TARGET:下半身下着１
	PRINTFORM %CALLNAME:TARGET%\@ !FLAG:70 ? が # から\@%SHOW_下半身下着(TARGET, 3)%を
	IF PALAM:TARGET:潤滑 >= PALAMLV:4
		PRINTFORM \@ !FLAG:70 ? 脱ぐ # 脱がせる\@
		IF GET_INT(TARGET, "下半身下着_ずらし不可", EQUIP:TARGET:下半身下着１, "透ける(前)") && !RAND:2
			PRINTFORMW と、%CALLNAME:TARGET%の%SLIT(TARGET)%はグチャグチャにぬかるみきっており、%SHOW_下半身下着(TARGET, 3)%は愛液を吸い込んでずっしりと重くなっている…	
		ELSE
			PRINTFORMW と、%SLIT(TARGET)%から溢れた愛液がダラダラと太ももを伝っている…
		ENDIF
	ELSEIF PALAM:TARGET:潤滑 >= PALAMLV:3
		PRINTFORM \@ !FLAG:70 ? 脱ぐ # 脱がせる\@
		IF GET_INT(TARGET, "下半身下着_ずらし不可", EQUIP:TARGET:下半身下着１, "透ける(前)") && !RAND:2
			PRINTFORMW と、%SLIT(TARGET)%と%SHOW_下半身下着(TARGET, 3)%の間で愛液が糸を引いた…
		ELSE
			PRINTFORMW と、%CALLNAME:TARGET%の%SLIT(TARGET)%ははっきりと濡れている…
		ENDIF
	ELSEIF PALAM:TARGET:潤滑 >= PALAMLV:2
		PRINTFORMW \@ !FLAG:70 ? 脱ぐ # 脱がせる\@と、%CALLNAME:TARGET%の%SLIT(TARGET)%はしっとりと湿っていた…
	ELSE
		PRINTFORMW \@ !FLAG:70 ? 脱いだ… # 脱がせた…\@
	ENDIF
ELSE
	IF PALAM:TARGET:潤滑 >= PALAMLV:4
		PRINTFORMW %CALLNAME:TARGET%の秘所はグチャグチャにぬかるみきっており、%SLIT(TARGET)%から溢れた愛液がダラダラと太ももを伝っている…
	ELSEIF PALAM:TARGET:潤滑 >= PALAMLV:3
		PRINTFORMW %CALLNAME:TARGET%の%SLIT(TARGET)%ははっきりと濡れている…
	ELSEIF PALAM:TARGET:潤滑 >= PALAMLV:2
		PRINTFORMW %CALLNAME:TARGET%の%SLIT(TARGET)%ははしっとりと湿っている…
	ELSE
		PRINTFORMW %CALLNAME:TARGET%の%SLIT(TARGET)%が露わになっている…
	ENDIF
ENDIF

SELECTCASE CFLAG:TARGET:妊娠経過日数
	CASE IS >= 60
		IF TALENT:TARGET:体型 == -2
			PRINTFORMW 臨月のお腹は幼い体とはアンバランスな程に膨らんでいる
		ELSE
			PRINTFORMW 臨月のお腹は大きく膨らんでいる
		ENDIF
	CASE IS >= 40
		PRINTFORMW %CALLNAME:TARGET%の体つきは以前より丸みを帯び、お腹ははっきりと膨らんでいる
	CASE IS >= 30
		PRINTFORMW 膨らみはじめた%CALLNAME:TARGET%のお腹を見ると、あらためて%CALLNAME:TARGET%を孕ませたということを実感する
	CASE IS >= 20
		PRINTFORMW %CALLNAME:TARGET%のお腹はまだ膨らんでもいないが、その胎内に自分の子を宿していると思うと感慨深いものがある
ENDSELECT

@MESSAGE_脱衣上
IF SHOW_TOP(TARGET) != ""
	IF SHOW_TOP(TARGET, 1) != ""
		PRINTFORM \@ FLAG:70 ? # %CALLNAME:TARGET%が\@%SHOW_TOP(TARGET,1)%を肌蹴\@ FLAG:70 ? させ、 # ると、 \@
		IF (SHOW_上半身下着(TARGET) != SHOW_TOP(TARGET,1)) && (SHOW_上半身下着(TARGET) != "")
			SIF !EQUIP:TARGET:上半身下着１ || !EQUIP:TARGET:上半身下着２
				PRINTFORM %SHOW_上半身下着(TARGET)%を脱\@ FLAG:70 ? がし # ぎ \@、
;		ELSE
;			SIF !EQUIP:TARGET:上半身下着１ || !EQUIP:TARGET:上半身下着２
;				PRINTFORM %SHOW_上半身下着(TARGET)%を脱\@ FLAG:70 ? がし # ぎ \@、
		ENDIF
		PRINTFORML %OPPAI_DESCRIPTION(TARGET, 1)%が露わになった
	ELSE
		PRINTFORM \@ FLAG:70 ? %CALLNAME:TARGET%から # %CALLNAME:TARGET%が \@%SHOW_TOP(TARGET,2)%
		IF SHOW_上半身下着(TARGET) != SHOW_TOP(TARGET,1) && SHOW_上半身下着(TARGET) != SHOW_TOP(TARGET,2)
			SIF !EQUIP:TARGET:上半身下着１ || !EQUIP:TARGET:上半身下着２
				PRINTFORM と%SHOW_上半身下着(TARGET)%
		ENDIF
		PRINTFORML を脱\@ FLAG:70 ? がし # ぎ \@、%OPPAI_DESCRIPTION(TARGET, 1)%が露わになった
	ENDIF
ELSE
	PRINTFORMW %CALLNAME:TARGET%の%OPPAI_DESCRIPTION(TARGET, 1)%が露わになっている
ENDIF

IF PALAM:TARGET:欲情 >= PALAMLV:3
	IF PALAM:TARGET:欲情 >= PALAMLV:6
		PRINTFORM その%NIPPLE_DESCRIPTION(TARGET)%は、
		IF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000 && ABL:Ｂ感覚 <= 5
			IF RAND:2 == 0
				PRINT まるで乳辱への期待に打ち震えるかのように
			ELSE
				PRINTFORM %CALLNAME:PLAYER%へ乳嬲りをせがむかの如く
			ENDIF
		ENDIF
		IF RAND:4 == 0
			PRINT 快感から厭らしく膨れ
		ELSEIF RAND:3 == 0
			PRINT ビンビンになって
		ELSEIF RAND:2 == 0
			PRINT 乳輪からこんもりと
		ELSE
			PRINT 乳輪ごとぷっくりと
		ENDIF
		PRINTFORMW 盛り上がっている…
	ELSEIF PALAM:TARGET:欲情 >= PALAMLV:3
		PRINTFORM その%NIPPLE_DESCRIPTION(TARGET)%は、
		IF RAND:3 == 0
			PRINT ツンと上向い
		ELSEIF RAND:2 == 0
			PRINT すっかり硬くなっ
		ELSE
			PRINT 勃起し
		ENDIF
		PRINTFORMW て、自己主張している…
	ENDIF
ENDIF

@MESSAGE_脱衣下(ARG)
SIF !(EQUIP:TARGET:スカート || EQUIP:TARGET:ズボン)
	RETURN
SIF (!ARG && EQUIP:TARGET:スカート) || EQUIP:TARGET:ズボン
	PRINTFORM \@ EQUIP:TARGET:ズボン ? %SHOW_BOTTOM(TARGET)% # %SHOW_BOTTOM(TARGET, 1)%\@を脱\@ FLAG:70 ? がせる # ぐ \@と、

PRINTFORM %UNDER_SKIRT_DESCRIPTION(TARGET)%
IF (EQUIP:TARGET:下半身下着１ || EQUIP:TARGET:下半身下着２)
	IF PALAM:潤滑 >= PALAMLV:3
		PRINTFORMW は愛液で染みが出来ている…
	ELSEIF PALAM:潤滑 >= PALAMLV:2
		PRINTFORMW が愛液で湿っている…
	ELSE
		PRINTFORMW が見れた…
	ENDIF
ELSE
	PRINTFORMW が見れた…
ENDIF
