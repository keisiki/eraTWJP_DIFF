;-------------------------------------------------
;80,手淫
;-------------------------------------------------
@MESSAGE_COM80
VARSET LOCAL
IF MASTER_POSE(SET_V,SET_P,1)
	LOCAL = MASTER_POSE(SET_V,SET_P,1)
ELSEIF MASTER_POSE(SET_A,SET_P,1)
	LOCAL = MASTER_POSE(SET_A,SET_P,1)
ENDIF
IF FLAG:70 == 1
	IF NOWEX:MASTER:射精
		IF LOCAL == TARGET
			PRINTFORML 時間を止めて犯している%CALLNAME:TARGET%の\@ MASTER_POSE(SET_V,SET_P,1) ? ヴァギナ # アナル \@から%TINKO(PLAYER)%を抜き、%CALLNAME:TARGET%の手に握らせ
		ELSEIF LOCAL
			PRINTFORML 時間を止めて犯している%CALLNAME:LOCAL%から%TINKO(PLAYER)%を抜き、%CALLNAME:TARGET%の手に握らせ
		ELSE
			PRINTFORML 時間の止まっている%CALLNAME:TARGET%の手に%TINKO(PLAYER)%を握らせ
		ENDIF
			PRINTFORML %CALLNAME:TARGET%の手を使って、射精寸前の%TINKO(PLAYER)%を前後にしごいた…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%は時間の止まっている%CALLNAME:TARGET%の手に%TINKO(PLAYER)%を握らせて擦りあげた…
	ENDIF
ELSEIF CFLAG:MASTER:イタズラ == 2
	IF NOWEX:MASTER:射精
		IF LOCAL == TARGET
			PRINTFORML 眠ったまま犯されている%CALLNAME:TARGET%の\@ MASTER_POSE(SET_V,SET_P,1) ? ヴァギナ # アナル \@から%TINKO(PLAYER)%を抜き、%CALLNAME:TARGET%の手に握らせ
		ELSEIF LOCAL
			PRINTFORML 眠ったまま犯されている%CALLNAME:LOCAL%から%TINKO(PLAYER)%を抜き、%CALLNAME:TARGET%の手に握らせ
		ELSE
			PRINTFORML 眠ったまま犯されている%CALLNAME:TARGET%の手に%TINKO(PLAYER)%を握らせ
		ENDIF
			PRINTFORML 射精寸前の%TINKO(PLAYER)%を前後にしごいた…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%は眠っている%CALLNAME:TARGET%の手に%TINKO(PLAYER)%を握らせて擦りあげた…
	ENDIF
ELSE
	IF NOWEX:MASTER:射精
		IF LOCAL == TARGET
			PRINTFORML 射精の直前に\@ MASTER_POSE(SET_V,SET_P,1) ? ヴァギナ # アナル \@から%TINKO(PLAYER)%を抜き%CALLNAME:TARGET%に握らせた…
		ELSEIF LOCAL
			PRINTFORML 射精の直前に%CALLNAME:LOCAL%から%TINKO(PLAYER)%を抜き%CALLNAME:TARGET%に握らせた…
		ELSE
			PRINTFORML 射精寸前の%TINKO(PLAYER)%を%CALLNAME:TARGET%に握らせた…
		ENDIF
		IF ABL:技巧 > 3
			IF ABL:精液中毒 >= 5
				PRINTFORML %CALLNAME:TARGET%は激しく%TINKO(PLAYER)%をしごきながら鈴口の前で口を開け精液を待ち構えている…
			ELSE
				PRINTFORML %CALLNAME:TARGET%は手馴れた手つきで%TINKO(PLAYER)%の敏感な部分を探し当て%CALLNAME:PLAYER%を射精へと導いた…
			ENDIF
		ELSE
			PRINTFORML %CALLNAME:TARGET%は戸惑いながらも鈴口の部分をじっと見つめ、%TINKO(PLAYER)%を前後にしごいている…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%は
		;恋慕か親愛
		SIF TALENT:恋慕
			PRINT 愛しむような表情で、
		;淫乱か娼婦
		SIF TALENT:淫乱
			PRINT 自分から
		SIF CFLAG:負け犬 && !TALENT:恋慕 && !TALENT:淫乱
			PRINTFORM 顔を背けながら
		PRINTFORML %CALLNAME:PLAYER%の%TINKO(PLAYER)%に指を絡めて擦りあげている…
	ENDIF
ENDIF
;-------------------------------------------------
;81,フェラチオ
;-------------------------------------------------
@MESSAGE_COM81
VARSET LOCAL
;お掃除用
IF TCVAR:MASTER:112 == 1
	LOCAL = MASTER_POSE(SET_V,SET_P,1)
ELSEIF TCVAR:MASTER:112 == 2
	LOCAL = MASTER_POSE(SET_A,SET_P,1)
ENDIF
IF MASTER_POSE(SET_V,SET_P,1)
	LOCAL:1 = MASTER_POSE(SET_V,SET_P,1)
ELSEIF MASTER_POSE(SET_A,SET_P,1)
	LOCAL:1 = MASTER_POSE(SET_A,SET_P,1)
ENDIF
IF FLAG:70 == 1
	;お掃除
	IF LOCAL
		PRINTFORML 時間の止まった%CALLNAME:LOCAL%の中にたっぷりと射精してから%TINKO(PLAYER)%を抜き、
		PRINTFORML 精液の付着した%TINKO(PLAYER)%を%CALLNAME:TARGET%の口に突き入れ、%TINKO(PLAYER)%を掃除させた…
	ELSEIF NOWEX:MASTER:射精
		IF LOCAL:1 == TARGET
			PRINTFORML 時間を止めて犯している%CALLNAME:TARGET%の\@ MASTER_POSE(SET_V,SET_P,1) ? ヴァギナ # アナル \@から%TINKO(PLAYER)%を抜き、%CALLNAME:TARGET%の口に突き入れ
		ELSEIF LOCAL:1
			PRINTFORML 時間を止めて犯している%CALLNAME:(LOCAL:1)%から%TINKO(PLAYER)%を抜き、%CALLNAME:TARGET%の口に突き入れ
		ELSE
			PRINTFORML 時間の止まっている%CALLNAME:TARGET%の口に%TINKO(PLAYER)%を突き入れ
		ENDIF
			PRINTFORML 射精寸前の%TINKO(PLAYER)%を喉奥に押し付けた…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%は時間の止まっている%CALLNAME:TARGET%の口に%TINKO(PLAYER)%を突き入れ、舌に擦りつけるように腰を動かした…
	ENDIF
	SIF TALENT:キス未経験
		PRINTFORML キスの経験もない%CALLNAME:TARGET%の可憐な唇を穢しているという事実は、%CALLNAME:MASTER%をいたく昂らせる……
ELSEIF CFLAG:MASTER:イタズラ == 2
	;お掃除
	IF LOCAL
		PRINTFORML 眠ったまま犯されている%CALLNAME:LOCAL%の中にたっぷりと射精してから%TINKO(PLAYER)%を抜き、
		PRINTFORML 精液の付着した%TINKO(PLAYER)%を%CALLNAME:TARGET%の口に突き入れ、%TINKO(PLAYER)%を掃除させた…
	ELSEIF NOWEX:MASTER:射精
		IF LOCAL:1 == TARGET
			PRINTFORML 眠ったまま犯されている%CALLNAME:TARGET%の\@ MASTER_POSE(SET_V,SET_P,1) ? ヴァギナ # アナル \@から%TINKO(PLAYER)%を抜き、%CALLNAME:TARGET%の口に突き入れ
		ELSEIF  LOCAL:1
			PRINTFORML 眠ったまま犯されている%CALLNAME:(LOCAL:1)%から%TINKO(PLAYER)%を抜き、%CALLNAME:TARGET%の口に突き入れ
		ELSE
			PRINTFORML 眠ったまま犯されている%CALLNAME:TARGET%の口に%TINKO(PLAYER)%を突き入れ
		ENDIF
			PRINTFORML 射精寸前の%TINKO(PLAYER)%を喉奥に押し付けた…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%は眠っている%CALLNAME:TARGET%の口に%TINKO(PLAYER)%を突き入れ、舌に擦りつけるように腰を動かした…
	ENDIF
ELSE
	;お掃除
	IF LOCAL
		PRINTFORML %CALLNAME:LOCAL%の中にたっぷりと射精してから%TINKO(PLAYER)%を抜き、
		PRINTFORML %TINKO(PLAYER)%に付着した精液を%CALLNAME:TARGET%に舐め取らせた…
		SIF TFLAG:8 && !TCVAR:TARGET:泥酔
			PRINTFORML %CALLNAME:TARGET%は尿道に残った精液まで残らずきれいにしてから\@ ABL:精液中毒 > 4 ? 名残を惜しむかのように # \@%TINKO(PLAYER)%から唇を離した…
	ELSEIF NOWEX:MASTER:射精
		IF LOCAL:1 == TARGET
			PRINTFORML 射精の直前に\@ MASTER_POSE(SET_V,SET_P,1) ? ヴァギナ # アナル \@から%TINKO(PLAYER)%を抜き%CALLNAME:TARGET%に咥えさせた…
		ELSEIF LOCAL:1
			PRINTFORML 射精の直前に%CALLNAME:(LOCAL:1)%から%TINKO(PLAYER)%を抜き%CALLNAME:TARGET%に咥えさせた…
		ELSE
			PRINTFORML 射精寸前の%TINKO(PLAYER)%を%CALLNAME:TARGET%に咥えさせた…
		ENDIF
		IF ABL:舌 > 2 && !TCVAR:TARGET:泥酔
			IF ABL:精液中毒 > 4
				PRINTFORML %CALLNAME:TARGET%は唇で亀頭を包んでからねっとりと舌を動かし%CALLNAME:PLAYER%に射精を促している…
			ELSE
				PRINTFORML %CALLNAME:TARGET%は%TINKO(PLAYER)%の敏感な部分に舌をはわせ%CALLNAME:PLAYER%に射精を促している…
			ENDIF
		ELSE
			PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の反応を伺いながら懸命に舌を動かしている…
		ENDIF
	ELSEIF TCVAR:TARGET:泥酔
		PRINTFORML 朦朧としている%CALLNAME:TARGET%の口に%TINKO(PLAYER)%を咥えさせた
		PRINTFORML やってから気付いたけど噛まれませんように……
	ELSE
		PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%の%TINKO(PLAYER)%に
		;恋慕
		SIF TALENT:恋慕
			PRINT 慈しむような表情で、
		;淫乱
		SIF TALENT:淫乱
			PRINT 自ら
		SIF CFLAG:負け犬 && !TALENT:恋慕 && !TALENT:淫乱
			PRINTFORM 涙目で
		PRINTFORML 舌を這わせている…
	ENDIF
ENDIF
;-------------------------------------------------
;82,パイズリ
;-------------------------------------------------
@MESSAGE_COM82
IF SELECTCOM == PREVCOM
	IF FLAG:70 == 1 || CFLAG:MASTER:イタズラ == 2
		PRINTFORM %CALLNAME:PLAYER%は\@ FLAG:70?時間を止めた #眠っている \@%CALLNAME:TARGET%の%OPPAI_DESCRIPTION(TARGET,1)%
		IF RAND:2
			IF RAND:3 == 0
				PRINTFORM の乳輪を親指でくすぐりながら
			ELSEIF RAND:2
				PRINTFORM の%NIPPLE_DESCRIPTION(TARGET,1)%を指で扱きながら
			ELSE
				PRINTFORM \@ TALENT:バストサイズ == -2 ? を手のひら全体で撫でまわしながら# を揉みしだきながら\@
			ENDIF
		ELSE
			PRINTFORM %TEXTR("を内側に寄せながら/を密着させながら/を締め付けながら/を圧迫しながら")%
		ENDIF
		SIF EXP:PLAYER:Ｖ性交経験 + EXP:PLAYER:Ａ性交経験 && RAND:2
			PRINTFORM %TEXTR("性交でもするかのように/乳を孕ませようと/まるで交尾をしているかの如く")%
		SIF RAND:2
			PRINTFORM 体力の続くまま
		IF ABL:PLAYER:サドっ気 && RAND:3 == 0
			PRINTFORM 荒々しく腰を動かし
		ELSE
			PRINTFORM %TEXTR("腰を激しく動かし/腰を前後に動かし")%
		ENDIF
		IF RAND:2 == 0
			PRINTFORML 犯し続けている…
		ELSE
			PRINTFORML 犯し続けた…
		ENDIF
	ELSE
		;超乳
		IF TALENT:バストサイズ == 3 && RAND:3
			PRINTFORML 規格外の%OPPAI_DESCRIPTION(TARGET,1)%は%CALLNAME:PLAYER%の指を飲み込み、持ち上げていないと自重によってデロリと垂れ下がる…
			PRINTFORML なので%CALLNAME:TARGET%は何度も%OPPAI_DESCRIPTION(TARGET)%を持ち上げ、その動作によって%CALLNAME:PLAYER%の%TINKO(PLAYER)%を扱き続けた…
		;貧乳
		ELSEIF TALENT:バストサイズ == -1
			PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%で懸命に%TINKO(PLAYER)%を擦り続けている…
		;絶壁
		ELSEIF TALENT:バストサイズ == -2
			PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%と%NIPPLE_DESCRIPTION(TARGET,1)%で必死に%TINKO(PLAYER)%を擦り続けている…
		;巨乳
		ELSEIF TALENT:バストサイズ == 1
			PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%で%TINKO(PLAYER)%を包み込んで擦りあげ続けている…
		;爆乳、超乳
		ELSEIF TALENT:バストサイズ >= 2
			PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%で%TINKO(PLAYER)%を包み隠すようにして擦りあげ続けている…
		ELSE
			PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%で%TINKO(PLAYER)%をはさんで擦りあげ続けている…
		ENDIF
	ENDIF
	RETURN 0
ENDIF

IF FLAG:70 == 1
	PRINTFORM %CALLNAME:PLAYER%は時間を止めた%CALLNAME:TARGET%の%OPPAI_DESCRIPTION(TARGET,1)%
	;貧乳
	IF TALENT:バストサイズ == -1
		PRINTFORM に、%TINKO(PLAYER)%を擦りつけ
	;絶壁
	ELSEIF TALENT:バストサイズ == -2
		PRINTFORM で、そそり立つ%NIPPLE_DESCRIPTION(TARGET,1)%に%TINKO(PLAYER)%を擦りつけ
	;普乳以上
	ELSE
		PRINTFORM に、%TINKO(PLAYER)%をはさませて擦り上げ
	ENDIF
	PRINTL た…
ELSEIF CFLAG:MASTER:イタズラ == 2
	PRINTFORM %CALLNAME:PLAYER%は眠っている%CALLNAME:TARGET%の
	;貧乳
	IF TALENT:バストサイズ == -1
		PRINTFORM %TINKO(PLAYER)%を擦りつけ
	;絶壁
	ELSEIF TALENT:バストサイズ == -2
		PRINTFORM そそり立つ%NIPPLE_DESCRIPTION(TARGET,1)%に、%TINKO(PLAYER)%を擦りつけ
	;巨乳
	ELSEIF TALENT:バストサイズ == 1
		PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%に、%TINKO(PLAYER)%をはさませて擦り上げ
	;爆乳
	ELSEIF TALENT:バストサイズ == 2
		PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%に、%TINKO(PLAYER)%をはさませて擦り上げ
	ELSE
		PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%に、%TINKO(PLAYER)%をはさませて擦り上げ
	ENDIF
	PRINTL た…
ELSE
	PRINTFORM %CALLNAME:TARGET%は
	;恋慕か親愛（愛欲の有無でちょっと分岐）
	SIF TALENT:愛欲
		PRINTFORM \@ TALENT:恋慕 ? うっとりとした # 蕩けた \@表情をしながら、
		SIF CFLAG:負け犬 && !TALENT:恋慕 && !TALENT:淫乱
			PRINTFORM 心底嫌そうな表情で
	IF ABL:TARGET:胸 > 2
	;胸3以上
		;胸で分岐させてサイズはSELECTCASEで分けてみる。大きさは違ってもその先に突起はあるんだよ
		SELECTCASE TALENT:バストサイズ
		;きょにゅー以上
		CASE IS > 0
			IF RAND:2 == 0
				PRINTFORML むっちりと詰まった肉と肉、%OPPAI_DESCRIPTION(TARGET,1)%の間に%TINKO(PLAYER)%を挟み込み、扱きあげる。
				PRINTL しっとりと包み込んで、柔らかくどこまでも硬い肉棒を受け入れて
				PRINTL 沈んでいくような心地よさは、まるで肉の海に蕩けるような感覚だ…
			ELSE
				PRINTFORML その%OPPAI_DESCRIPTION(TARGET,1)%を押し付け、たっぷりと%OPPAI_DESCRIPTION(TARGET)%の重圧を加えた中に、
				PRINTFORML %CALLNAME:PLAYER%の%TINKO(PLAYER)%を導いた。
				PRINTFORML %CALLNAME:TARGET%の肉が柔らかく圧倒的な量感で触れていく心地に、%CALLNAME:PLAYER%は飲み込まれていく…
			ENDIF
			PRINTL ぬめる肉と肉は押しこめば押しこむほど硬いペニスをどこまでも受け入れ、沈み込むほど柔らかい…
			SIF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000
				PRINTFORML その中で桃色付いた%OPPAI_DESCRIPTION(TARGET,1)%の先端、%NIPPLE_DESCRIPTION(TARGET,1)%が時折、コリコリと竿やカリを擦ってくる…
		;ひんぬー以下
		CASE IS < 0
			PRINTFORML 物理的なハンデを経験と技で補おうと%OPPAI_DESCRIPTION(TARGET,1)%を使い%TINKO(PLAYER)%に奉仕している。
			SIF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000
				PRINTFORML 上気した胸板の先にある%NIPPLE_DESCRIPTION(TARGET,1)%で竿やカリを擦るように丁寧に誘導してくる。
		;ふにゅぅ
		CASEELSE
			PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%で%TINKO(PLAYER)%を挟み、扱き上げる。
			SIF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000
				PRINTFORML 時折汗ばんだ%OPPAI_DESCRIPTION(TARGET,1)%の頂き、%NIPPLE_DESCRIPTION(TARGET,1)%に竿やカリを擦れさせては甘い声をあげる。
		ENDSELECT
	ELSEIF ABL:TARGET:胸 <= 2
	;胸2以下
		;貧乳
		IF TALENT:バストサイズ == -1
			PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%で懸命に%TINKO(PLAYER)%を
		;絶壁
		ELSEIF TALENT:バストサイズ == -2
			PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%と%NIPPLE_DESCRIPTION(TARGET,1)%で必死に%TINKO(PLAYER)%を
		;巨乳
		ELSEIF TALENT:バストサイズ == 1
			PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%で%TINKO(PLAYER)%を包み込んで
		;爆乳、超乳
		ELSEIF TALENT:バストサイズ >= 2
			PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%で%TINKO(PLAYER)%を包み隠すようにして
		ELSE
			PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%で%TINKO(PLAYER)%をはさんで
		ENDIF
		PRINTL 擦りあげている…
	ELSE

	ENDIF
	
	;恋慕又は愛欲
	IF IS_LOVER(TARGET) && EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 30000 && RAND:3 == 0
		PRINTFORML 気分が高揚でもしてきたのか、%CALLNAME:TARGET%は激しく身体を揺さぶり、
		PRINTFORML %CALLNAME:PLAYER%の%TINKO(PLAYER)%を%OPPAI_DESCRIPTION(TARGET)%で徹底的に扱き上げながら、熱に浮かされてるかのように
		PRINTFORML 自分のパイズリはどうだ？　気持ちいいか？
		PRINTFORML また%SAMEN_DESCRIPTION(PLAYER)%を出すのか？　かけるのか？　注ぎこむのか？
		PRINTFORML と、快楽に心をゆだねた艶やかな様相で問いかけてくる…
	ENDIF
	SELECTCASE ABL:TARGET:胸
	CASE IS < 2
		PRINTFORM %CALLNAME:TARGET%には悪いが、たどたどしい奉仕で
		SIF TALENT:バストサイズ >= 1
			PRINT 視覚的にはともかく
		PRINTL あんまり気持ちよくない…
	CASE IS  > 2
		PRINTFORML %CALLNAME:TARGET%はこなれた奉仕で%CALLNAME:PLAYER%を悦ばせた…
	CASEELSE
		PRINTFORML %CALLNAME:TARGET%の奉仕は%CALLNAME:PLAYER%を気持よくさせている…
	ENDSELECT
ENDIF
;-------------------------------------------------
;83,素股
;-------------------------------------------------
@MESSAGE_COM83
IF FLAG:70 == 1
	PRINTFORML %CALLNAME:PLAYER%は時間を止めた%CALLNAME:TARGET%の股に%TINKO(PLAYER)%を挟ませて
	;欲情のPALAMLV4以上
	SIF PALAM:潤滑 >= PALAMLV:3
		PRINT ぐしょぐしょに濡れた
	;小柄体型
	IF TALENT:体型 < 0
		PRINT 幼いワレメ
	ELSE
		PRINT 陰唇
	ENDIF
	PRINTFORML に擦りつけた…
ELSE
	PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%の%TINKO(PLAYER)%を股にはさみ、
	;淫乱か娼婦＋欲情のPALAMLV5以上
	IF TALENT:淫乱 && PALAM:欲情 >= PALAMLV:5
		PRINTFORM 快楽に溺れきった表情で%CALLNAME:PLAYER%を見つめながら
	;恋慕か親愛＋欲情のPALAMLV5以上
	ELSEIF TALENT:恋慕 && PALAM:欲情 >= PALAMLV:5
		PRINT 今にもとろけそうな表情を浮かべながら
	;欲情のPALAMLV4以上
	ELSEIF PALAM:欲情 >= PALAMLV:4
		PRINT 口の端からよだれをこぼしながら
	;恋慕か親愛
	ELSEIF TALENT:恋慕
		PRINTFORM 陶酔しきった瞳で%CALLNAME:PLAYER%を見つめながら
	;淫乱か娼婦
	ELSEIF TALENT:淫乱
		PRINTFORM 淫猥な目つきで挑発しながら
	ELSEIF CFLAG:負け犬
		PRINTFORM 心底嫌そうな表情で
	;恥薄い
	ELSEIF TALENT:羞恥心 < 0
		PRINTFORM 大胆に腰を動かしながら
	;感情乏しい
	ELSEIF TALENT:感情乏しい
		PRINTFORM うっすらと上気した顔で
	ELSE
		PRINT 恥ずかしそうに
	ENDIF
	;小柄体型
	IF TALENT:体型 < 0
		PRINT 幼いワレメを
	ELSE
		PRINT 陰唇を
	ENDIF
	PRINTFORML %TINKO(PLAYER)%に擦りつけている…
ENDIF
;-------------------------------------------------
;84,泡踊り
;-------------------------------------------------
@MESSAGE_COM84
PRINTFORM %CALLNAME:TARGET%はソープを泡立たせて自分に塗りたくり、
;恋慕か親愛
SIF TALENT:恋慕
	PRINT うっとりとした表情をしつつ
;小柄体型
IF TALENT:体型 < 0
	PRINTFORM %CALLNAME:PLAYER%に小さな身体を目一杯擦りつけて
;巨乳か爆乳＋小柄体型でない
ELSEIF TALENT:バストサイズ > 0
	PRINTFORM %CALLNAME:PLAYER%に%OPPAI_DESCRIPTION(TARGET)%とふくよかな肌を擦りつけて
ELSE
	PRINTFORM %CALLNAME:PLAYER%に全身を擦りつけて
ENDIF
PRINTL 愛撫した…
;-------------------------------------------------
;85,足扱き
;-------------------------------------------------
@MESSAGE_COM85
IF FLAG:70 == 1
	PRINTFORML %CALLNAME:PLAYER%は時間を止めた%CALLNAME:TARGET%の足裏に%TINKO(PLAYER)%を擦り付けている…
ELSEIF CFLAG:MASTER:イタズラ == 2
	PRINTFORML %CALLNAME:PLAYER%は眠っている%CALLNAME:TARGET%の足裏に%TINKO(PLAYER)%を擦り付けている…
ELSE
	PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%の%TINKO(PLAYER)%を足裏で
	;サド
	SIF TALENT:サド
		PRINT いたぶりながら
	;恋慕か親愛
	SIF TALENT:恋慕
		PRINT 丹念に
	PRINTFORML 擦りあげている…
ENDIF
;-------------------------------------------------
;86,足舐め強制
;-------------------------------------------------
@MESSAGE_COM86
;野外プレイ中
IF TEQUIP:野外プレイ
	;昼
	IF TIME == 0
		PRINT 晴天の下で、
	;夜
	ELSE
		PRINT 月の光の中、
	ENDIF
	;臆病、プライド高い、恥じらいのいずれか
	IF TALENT:度胸 < 0 || TALENT:プライド > 0 || TALENT:羞恥心 > 0
		PRINTL この痴態を誰かに見られていないのかとびくびくしながら
	ELSE
		PRINTL 風に撫でられて素肌を僅かに震わせて
	ENDIF
;媚薬有効
ELSEIF TCVAR:媚薬
	PRINT 視線を宙に漂わせつつ
ENDIF
PRINTFORML %CALLNAME:TARGET%は差し出された%CALLNAME:PLAYER%の足に舌を這わせている…
;-------------------------------------------------
;88,バキュームフェラ
;-------------------------------------------------
@MESSAGE_COM88
	PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%の%TINKO(PLAYER)%を根本まで咥え、
	PRINTL そのまま激しい勢いで陰茎に吸いついている…
;-------------------------------------------------
;89,ゴム精飲
;-------------------------------------------------
@MESSAGE_COM89
IF FLAG:70
	PRINTFORML %CALLNAME:TARGET%の口の中で使用済みコンドームをひっくり返して、\@ TFLAG:ゴム内 > 150 ? 大量の # \@%SAMEN_DESCRIPTION(PLAYER)%をご馳走した…
	PRINTFORML %CALLNAME:PLAYER%の味をしっかり覚えてもらえるよう、%CALLNAME:PLAYER%はコンドームを裏返して頬の裏に擦りつけ、一滴残らず%SAMEN_DESCRIPTION(PLAYER)%を味あわせた…
ELSEIF CFLAG:MASTER:イタズラ == 2
	PRINTFORML %CALLNAME:TARGET%の口の中で使用済みコンドームをひっくり返して、\@ TFLAG:ゴム内 > 150 ? 大量の # \@%SAMEN_DESCRIPTION(PLAYER)%をご馳走した…
	PRINTFORML %CALLNAME:PLAYER%の味をしっかり覚えてもらえるよう、%CALLNAME:PLAYER%はコンドームを裏返して頬の裏に擦りつけ、一滴残らず%SAMEN_DESCRIPTION(PLAYER)%を味あわせた…
ELSE
	PRINTFORM %CALLNAME:TARGET%の頬に、
	SELECTCASE TFLAG:ゴム内
		CASE IS > 300
			PRINT はちきれんばかりに
		CASE IS > 150
			PRINT 先端にたっぷりと
	ENDSELECT
		PRINTFORML 子種の溜まったコンドームを押し当てた……
	;精液中毒5未満
	IF ABL:精液中毒 < 5
		PRINTFORML おずおずと口を開けた%CALLNAME:TARGET%の舌の上に精液をぶちまけると、%CALLNAME:TARGET%はそのまま嚥下した……
	ELSE
		PRINTFORML %CALLNAME:TARGET%は恍惚とした顔で、口内に流し込まれたゴム臭い精液を咀嚼するように味わっている……
		SIF ABL:精液中毒 >= 7
			PRINTFORML 精液を飲み干した後も、%CALLNAME:TARGET%は名残惜しげに裏返したコンドームをしゃぶっていた……
	ENDIF
ENDIF
;-------------------------------------------------
;90,アナル愛撫される
;-------------------------------------------------
@MESSAGE_COM90
IF TEQUIP:PLAYER:0 > 1
	PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%のアナルを服の上から
ELSE
	PRINTFORM %CALLNAME:TARGET%は%CALLNAME:PLAYER%のアナルを
ENDIF

IF ABL:PLAYER:Ａ感覚 <= 1
	PRINT 指の腹で撫でるように
ELSEIF ABL:PLAYER:Ａ感覚 <= 3
	PRINT 解きほぐすように
ELSE
	PRINT 指をねじ込むようにして
ENDIF

PRINTFORML 愛撫した…

IF ABL:PLAYER:Ａ感覚 <= 1
	PRINTFORML %CALLNAME:PLAYER%はその感触にまだ違和感を感じている…
ELSEIF ABL:PLAYER:Ａ感覚 <= 3
	PRINTFORML %CALLNAME:PLAYER%は異物感に背筋を震わせている…
ELSE
	PRINTFORML %CALLNAME:PLAYER%は恍惚とした表情になってされるがままになっている…
ENDIF

;-------------------------------------------------
;91,アナル舐めされる
;-------------------------------------------------
@MESSAGE_COM91
PRINTFORML %CALLNAME:TARGET%は%CALLNAME:PLAYER%のアナルに唇を這わせた
IF ABL:奉仕精神 >= 3
	PRINTFORML 皺を舐め広げられ、肛腔の中までも犯されて%CALLNAME:PLAYER%は思わず身を震わせた…
ELSE
	PRINTFORML 敏感な粘膜を舐めあげられ、%CALLNAME:PLAYER%は身を震わせた…
ENDIF
;-------------------------------------------------
;96,パイズリする
;-------------------------------------------------
@MESSAGE_COM96
IF FLAG:70 == 1
	PRINTFORM %CALLNAME:PLAYER%は時間を止めた%CALLNAME:TARGET%の%TINKO(TARGET)%を、
ELSEIF CFLAG:MASTER:イタズラ == 2
	PRINTFORM %CALLNAME:PLAYER%は眠っている%CALLNAME:TARGET%の%TINKO(TARGET)%を、
ELSE
	PRINTFORM %CALLNAME:PLAYER%は%CALLNAME:TARGET%の%TINKO(TARGET)%を、
ENDIF
PRINTFORML 自分の%OPPAI_DESCRIPTION(PLAYER,1)%に\@ TALENT:PLAYER:バストサイズ <= -1? 懸命になって擦り付け# はさませて擦り上げ\@た…
;-------------------------------------------------
;120,クンニ強制
;-------------------------------------------------
@MESSAGE_COM120
PRINTFORML %CALLNAME:TARGET%に%CALLNAME:PLAYER%の陰唇を舐めさせた…
;-------------------------------------------------
;121,貝あわせ
;-------------------------------------------------
@MESSAGE_COM121
IF FLAG:70 == 1
	PRINTFORM 時間を止めた%CALLNAME:TARGET%の性器に性器を擦りつけた…
ELSEIF CFLAG:MASTER:イタズラ == 2
	PRINTFORM 眠っている%CALLNAME:TARGET%の性器に性器を擦りつけた…
ELSE
	PRINTFORML %CALLNAME:PLAYER%と%CALLNAME:TARGET%は互いの性器を擦りあわせた…
ENDIF
;-------------------------------------------------
;122,助手を犯させる
;-------------------------------------------------
@MESSAGE_COM122
PRINTFORML %CALLNAME:TARGET%に%CALLNAME:PLAYER%を抱かせる事にした…
;-------------------------------------------------
;123,足コキする
;-------------------------------------------------
@MESSAGE_COM123
PRINTFORM %CALLNAME:PLAYER%は
SIF FLAG:70 == 1
	PRINTFORM 時間を止めた
SIF CFLAG:MASTER:イタズラ == 2
	PRINTFORM 眠っている
PRINTFORM %CALLNAME:TARGET%の%TINKO(TARGET)%を
;調教者がサド
SIF TALENT:PLAYER:サド
	PRINT 踏みつけるように
PRINTL 足でシゴいている…

;-------------------------------------------------
;124,手淫する
;-------------------------------------------------
@MESSAGE_COM124
PRINTFORM %CALLNAME:PLAYER%は
SIF TALENT:恋人
	PRINT 愛しむような表情で、
SIF FLAG:70 == 1
	PRINT 時間を止めた
SIF CFLAG:MASTER:イタズラ == 2
	PRINT 眠っている
PRINTFORM %CALLNAME:TARGET%の%TINKO(TARGET)%を
IF TEQUIP:33 || TEQUIP:34
	PRINT エプロンで包み扱き
ELSEIF TEQUIP:0 > 1
	PRINT 下着越しに擦り
ELSE
	PRINT 手に取り指を絡めて扱き
ENDIF
PRINTL 上げた…
;-------------------------------------------------
;125,愛撫させる
;-------------------------------------------------
@MESSAGE_COM125
PRINTFORM %CALLNAME:PLAYER%は
SIF FLAG:70 == 1
	PRINTFORM 時間を止めた
PRINTFORML %CALLNAME:TARGET%の手を取り自らの身体に導いた…
;-------------------------------------------------
;126,乳の揉み合い
;-------------------------------------------------
@MESSAGE_COM126
PRINTFORML %CALLNAME:PLAYER%と%CALLNAME:TARGET%は互いの胸を揉みあった…
;-------------------------------------------------
;140,イラマチオ
;-------------------------------------------------
@MESSAGE_COM140
IF CFLAG:MASTER:イタズラ == 2
	PRINTFORML %CALLNAME:PLAYER%は起こさないように慎重に%CALLNAME:TARGET%の喉奥まで%TINKO(PLAYER)%を突き入れた…
ELSE
	PRINTFORML %CALLNAME:PLAYER%は乱暴に%CALLNAME:TARGET%の喉まで突き入れた…
ENDIF
;-------------------------------------------------
;145,アナル奉仕強制
;-------------------------------------------------
@MESSAGE_COM145
PRINTFORML %CALLNAME:PLAYER%は、%CALLNAME:TARGET%にアナルを舐めさせた…

;-------------------------------------------------
;150,胸の谷間を犯す
;-------------------------------------------------
@MESSAGE_COM150
IF SELECTCOM == PREVCOM
	PRINTFORM %CALLNAME:PLAYER%は
	SIF FLAG:70 == 1
		PRINTFORM 時間を止めた
	SIF CFLAG:MASTER:イタズラ == 2
		PRINTFORM 眠っている
	PRINTFORM %CALLNAME:TARGET%の%OPPAI_DESCRIPTION(TARGET,1)%
	IF RAND:2
		IF RAND:3 == 0
			PRINTFORM の乳輪を親指でくすぐりながら
		ELSEIF RAND:2
			PRINTFORM の%NIPPLE_DESCRIPTION(TARGET,1)%を指で扱きながら
		ELSE
			PRINTFORM を揉みしだきながら
		ENDIF
	ELSE
		IF RAND:4 == 0
			PRINTFORM を内側に寄せながら
		ELSEIF RAND:3 == 0
			PRINTFORM を密着させながら
		ELSEIF RAND:2
			PRINTFORM を締め付けながら
		ELSE
			PRINTFORM を圧迫しながら
		ENDIF
	ENDIF

	IF EXP:PLAYER:Ｖ性交経験 + EXP:PLAYER:Ａ性交経験 && RAND:2
		IF RAND:3 == 0
			PRINT 性交でもするかのように
		ELSEIF RAND:2
			PRINTFORM 乳を孕ませようと
		ELSE
			PRINT まるで交尾をしているかの如く
		ENDIF
	ENDIF
	SIF RAND:2
		PRINTFORM 体力の続くまま
	IF ABL:PLAYER:サドっ気 && RAND:3 == 0
		PRINTFORML 荒々しく腰を動かし
	ELSEIF RAND:2
		PRINTFORML 腰を激しく動かし
	ELSE
		PRINTFORML 腰を前後に動かし
	ENDIF
	
	IF RAND:3
		PRINTFORM %OPPAI_DESCRIPTION(TARGET)%を
	ELSE
		PRINTFORM %OPPAI_DESCRIPTION(TARGET)%の谷間を
	ENDIF
	
	IF RAND:2 == 0
		PRINTFORML 犯し続けている…
	ELSE
		PRINTFORML 犯し続けた…
	ENDIF
	RETURN 0
ENDIF

PRINTFORM %CALLNAME:PLAYER%は
SIF FLAG:70 == 1
	PRINTFORM 時間を止めた
SIF CFLAG:MASTER:イタズラ == 2
	PRINTFORM 眠っている
PRINTFORM %CALLNAME:TARGET%の
;貧乳
IF TALENT:バストサイズ == -1
	PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%に%TINKO(PLAYER)%を擦りつけ
;巨乳
ELSEIF TALENT:バストサイズ == 1
	PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%に%TINKO(PLAYER)%をはさませて擦り上げ
;爆乳
ELSEIF TALENT:バストサイズ == 2
	PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%に%TINKO(PLAYER)%をはさませて擦り上げ
;超乳
ELSEIF TALENT:バストサイズ == 3
	PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%に%TINKO(PLAYER)%を突っ込み擦り上げ
ELSE
	PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%に%TINKO(PLAYER)%をはさませて擦り上げ
ENDIF
PRINTL る…

PRINTFORM %CALLNAME:PLAYER%は%CALLNAME:TARGET%の%OPPAI_DESCRIPTION(TARGET)%
IF RAND:2
	IF RAND:3 == 0
		PRINTFORM の乳輪を親指でくすぐりながら
	ELSEIF RAND:2
		PRINTFORM の%NIPPLE_DESCRIPTION(TARGET,1)%を指で扱きながら
	ELSE
		PRINTFORM を揉みしだきながら
	ENDIF
ELSE
	IF RAND:4 == 0
		PRINTFORM を内側に寄せながら
	ELSEIF RAND:3 == 0
		PRINTFORM を密着させながら
	ELSEIF RAND:2
		PRINTFORM を締め付けながら
	ELSE
		PRINTFORM を圧迫しながら
	ENDIF
ENDIF

IF EXP:PLAYER:Ｖ性交経験 + EXP:PLAYER:Ａ性交経験 && RAND:2
	IF RAND:3 == 0
		PRINT 性交でもするかのように
	ELSEIF RAND:2
		PRINTFORM 乳を孕ませようと
	ELSE
		PRINT まるで交尾をしているかの如く
	ENDIF
ENDIF

IF ABL:PLAYER:サドっ気 && RAND:3 == 0
	PRINTFORMW 荒々しく腰を動かした…
ELSEIF RAND:2
	PRINTFORMW 腰を激しく動かした…
ELSE
	PRINTFORMW 腰を前後に動かした…
ENDIF

IF !FLAG:70 && CFLAG:MASTER:イタズラ != 2
	PRINTFORM %CALLNAME:TARGET%は胸の谷間を犯され
	;恋慕か親愛（愛欲の有無でちょっと分岐）
	IF TALENT:愛欲
		PRINTFORML \@ TALENT:恋慕 ? うっとりとした # 蕩けた \@表情をしている…
	ELSE
		PRINTFORML ている…
	ENDIF
	IF ABL:PLAYER:技巧 > 2
	;プレイヤーの技巧3以上
		;胸で分岐させてサイズはSELECTCASEで分けてみる。大きさは違ってもその先に突起はあるんだよ
		SELECTCASE TALENT:バストサイズ
		;きょにゅー以上
		CASE IS > 0
			IF RAND:2 == 0
				PRINTFORML むっちりと詰まった肉と肉、%OPPAI_DESCRIPTION(TARGET,1)%の間に%TINKO(PLAYER)%を挟み込み、扱きあげる。
				PRINTL しっとりと包み込んで、柔らかくどこまでも硬い肉棒を受け入れて
				PRINTL 沈んでいくような心地よさは、まるで肉の海に蕩けるような感覚だ…
			ELSE
				PRINTFORML その%OPPAI_DESCRIPTION(TARGET,1)%に%TINKO(PLAYER)%を押し付け、たっぷりと%OPPAI_DESCRIPTION(TARGET)%の重圧を加えた中に、
				PRINTFORML %CALLNAME:PLAYER%の%TINKO(PLAYER)%を突きこむ。
				PRINTFORML %CALLNAME:TARGET%の肉が柔らかく圧倒的な量感で触れていく心地に、%CALLNAME:PLAYER%は飲み込まれていく…
			ENDIF
			PRINTL ぬめる肉と肉は押しこめば押しこむほど硬いペニスをどこまでも受け入れ、沈み込むほど柔らかい…
			SIF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000
				PRINTFORML その中で桃色付いた%OPPAI_DESCRIPTION(TARGET,1)%の先端、%NIPPLE_DESCRIPTION(TARGET,1)%が時折、コリコリと竿やカリを擦ってくる…
		;ひんぬー以下
		CASE IS < 0
			PRINTFORML 物理的なハンデを経験と技で補おうと%OPPAI_DESCRIPTION(TARGET,1)%を使い%TINKO(PLAYER)%に奉仕している。
			SIF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000
				PRINTFORML 上気した胸板の先にある%NIPPLE_DESCRIPTION(TARGET,1)%で竿やカリを擦るように丁寧に誘導してくる。
		;ふにゅぅ
		CASEELSE
			PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%で%TINKO(PLAYER)%を挟み、扱き上げる。
			SIF EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 10000
				PRINTFORML 時折汗ばんだ%OPPAI_DESCRIPTION(TARGET,1)%の頂き、%NIPPLE_DESCRIPTION(TARGET,1)%に竿やカリを擦れさせては甘い声をあげる。
		ENDSELECT
	ELSEIF ABL:PLAYER:技巧 <= 2
	;胸2以下
		;貧乳
		IF TALENT:バストサイズ == -1
			PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%で懸命に%TINKO(PLAYER)%を
		;巨乳
		ELSEIF TALENT:バストサイズ == 1
			PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%で%TINKO(PLAYER)%を包み込んで
		;爆乳、超乳
		ELSEIF TALENT:バストサイズ >= 2
			PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%で%TINKO(PLAYER)%を包み隠すようにして
		ELSE
			PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%で%TINKO(PLAYER)%をはさんで
		ENDIF
		PRINTL 擦りあげている…
	ELSE

	ENDIF
	;恋慕又は愛欲
	IF IS_LOVER(TARGET) && EX:Ｂ絶頂 * 10000 + PALAM:快Ｂ + PALAM:欲情 >= 30000 && RAND:3 == 0
		PRINTFORML 気分が高揚でもしてきたのか、%CALLNAME:TARGET%は激しく身体を揺さぶり、
		PRINTFORML %CALLNAME:PLAYER%の%TINKO(PLAYER)%を%OPPAI_DESCRIPTION(TARGET)%で徹底的に扱き上げながら、熱に浮かされてるかのように
		PRINTFORML 自分のパイズリはどうだ？　気持ちいいか？
		PRINTFORML また%SAMEN_DESCRIPTION(PLAYER)%を出すのか？　かけるのか？　注ぎこむのか？
		PRINTFORML と、快楽に心をゆだねた艶やかな様相で問いかけてくる…
	ENDIF
ENDIF

;-------------------------------------------------
;180,ローション,
;-------------------------------------------------
@MESSAGE_COM180
GETBIT STAIN:1, 7
IF RESULT
	PRINTFORML %CALLNAME:TARGET%にチョコレートをぬりたくった…
ELSEIF TALENT:TARGET:体型 == -5
	PRINTFORML %CALLNAME:TARGET%をローション漬けにした
	PRINTFORML %CALLNAME:TARGET%にとっては一回分でも溺れるほどの量だ
ELSE
	PRINTFORML %CALLNAME:TARGET%にローションをぬりたくった…
ENDIF
;-------------------------------------------------
;181,媚薬,
;-------------------------------------------------
@MESSAGE_COM181
IF CFLAG:MASTER:イタズラ == 2
	PRINTFORM 眠っている%CALLNAME:TARGET%に
	SIF ABL:PLAYER:技巧 >= 3
		PRINT 口移しで
	PRINT ゆっくりと
	PRINTFORML 媚薬を飲ませた
	PRINTFORML %CALLNAME:TARGET%は眠ったまま肌を紅潮させて股をもじもじと動かしている…
ELSE
	PRINTFORM %CALLNAME:TARGET%に
;調教者の技巧LV3以上
SIF ABL:PLAYER:技巧 >= 3
	PRINT 口移しで
;従順LV2以下
SIF ABL:従順 <= 2
	PRINT 無理やり
	PRINTFORML 媚薬を飲ませた
	PRINTFORML %CALLNAME:TARGET%は荒い息をついて肌を紅潮させている…
ENDIF
;-------------------------------------------------
;182,利尿剤,
;-------------------------------------------------
@MESSAGE_COM182
IF CFLAG:MASTER:イタズラ == 2
	PRINTFORM 眠っている%CALLNAME:TARGET%に
	SIF ABL:PLAYER:技巧 >= 3
		PRINT 口移しで
	PRINT ゆっくりと
	PRINTFORML 利尿剤を飲ませた
	PRINTFORML %CALLNAME:TARGET%は尿意を感じているのか股をもじもじと動かしている…
ELSE
PRINTFORM %CALLNAME:TARGET%に
;調教者の技巧LV3以上
SIF ABL:PLAYER:技巧 >= 3
	PRINT 口移しで
;従順LV2以下
SIF ABL:従順 <= 2
	PRINT 無理やり
PRINTFORML 利尿剤を飲ませた
PRINTFORML %CALLNAME:TARGET%は荒い息をつき、涙を浮かべて尿意に耐えている…
ENDIF
;-------------------------------------------------
;183,ビデオカメラ,
;-------------------------------------------------
@MESSAGE_COM183
;解除時
IF !TEQUIP:ビデオ撮影
	PRINTFORML %CALLNAME:TARGET%の撮影をやめた…
ELSE
	PRINTFORML ビデオカメラで%CALLNAME:TARGET%の撮影を始めた…
ENDIF
;-------------------------------------------------
;184,野外プレイ,
;-------------------------------------------------
@MESSAGE_COM184
;解除時
IF !TEQUIP:野外プレイ
	PRINTFORML 部屋に戻ってきた…
ELSE
	PRINTFORML %CALLNAME:TARGET%に首輪をつけて外に連れ出した…
ENDIF
;-------------------------------------------------
;185,助手を犯す,
;-------------------------------------------------
@MESSAGE_COM185
;前回の調教が助手を犯すであり、抜かないフラグがある場合
IF TFLAG:60 && PREVCOM == 56
	PRINTFORML %CALLNAME:TARGET%に見せつけながら、%CALLNAME:ASSI%を犯し続けた…
ELSE
	PRINTFORM %CALLNAME:TARGET%に見せつけながら、%CALLNAME:ASSI%の
	IF EXP:ASSI:Ｖ経験 == 0
		PRINT 初めてを
	ELSE
		PRINT ヴァギナを
	ENDIF
	PRINTL 貫いていった…
ENDIF
;-------------------------------------------------
;186,羞恥プレイ,
;-------------------------------------------------
@MESSAGE_COM186
;解除時
IF !TEQUIP:羞恥プレイ
	PRINTFORML %CALLNAME:TARGET%を連れて鏡の前から離れた…
ELSE
	PRINTFORML %CALLNAME:TARGET%を鏡の前に連れて来た…
ENDIF
;-------------------------------------------------
;187,お風呂場プレイ,
;-------------------------------------------------
@MESSAGE_COM187
IF CFLAG:MASTER:現在位置 == OMANEKIBEYA()
	;解除時
	IF !TEQUIP:お風呂場プレイ
		PRINTFORML お風呂場から帰ってきた…
		STR:98 = %CALLNAME:TARGET%の部屋
	ELSE
		PRINTFORML %CALLNAME:TARGET%をお風呂場に連れていった…
		STR:98 = %CALLNAME:TARGET%のお風呂
	ENDIF
ELSE
	;解除時
	IF !TEQUIP:お風呂場プレイ
		PRINTFORML プールを片付けた…
	ELSE
		PRINTFORML プールに温水を張った…
	ENDIF
ENDIF
;-------------------------------------------------
;188,シャワー,
;-------------------------------------------------
@MESSAGE_COM188
IF !TEQUIP:シャワー
	PRINTFORML %CALLNAME:TARGET%へのシャワーを止めた…
ELSE
	PRINTFORML %CALLNAME:TARGET%にシャワーを浴びさせた…
ENDIF
;-------------------------------------------------
;189,新妻プレイ,
;-------------------------------------------------
@MESSAGE_COM189
IF !TEQUIP:新妻プレイ
	PRINTFORML %CALLNAME:TARGET%のエプロンを脱がした…
ELSE
	PRINTFORML %CALLNAME:TARGET%にエプロンをつけた…
ENDIF
;-------------------------------------------------
;190,エプロンをする,
;-------------------------------------------------
@MESSAGE_COM190
IF !TEQUIP:エプロン
	PRINTFORML エプロンを外した
ELSE
	PRINTFORML エプロンを着けた
ENDIF
;-------------------------------------------------
;191,睡眠薬,
;-------------------------------------------------
@MESSAGE_COM191
IF CFLAG:MASTER:イタズラ == 2
	PRINTFORML 眠っている%CALLNAME:TARGET%のアナルにゆっくりと睡眠薬を押し込んだ
	PRINTFORML %CALLNAME:TARGET%は直腸から吸収した睡眠薬によって昏睡状態へと陥った
ELSE
	PRINTFORML 時間の止まっている%CALLNAME:TARGET%のアナルにゆっくりと睡眠薬を押し込んだ
ENDIF
;-------------------------------------------------
;192,排卵誘発剤,
;-------------------------------------------------
@MESSAGE_COM192
IF CFLAG:MASTER:イタズラ == 2
	PRINTFORML 眠っている%CALLNAME:TARGET%の口に排卵誘発剤を含ませた
ELSE
	PRINTFORML %CALLNAME:TARGET%に排卵誘発剤を飲んでもらった
ENDIF