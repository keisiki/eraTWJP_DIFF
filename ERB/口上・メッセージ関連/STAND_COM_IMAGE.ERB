; 立ち絵追加リソースセット関連
@DISPLAY_COM_IMAGE(リソース名, COMNO)
#DIMS リソース名
#DIM COMNO
IF !FLAG:立ち絵種類 && CHECK_NUDE(NO:TARGET)
	リソース名 = 立ち絵_裸_COM_{NO:TARGET}_{COMNO}
	;PRINTFORML うふふ画像存在チェック%リソース名%
	IF SPRITECREATED(リソース名)
		CALL 画像表示(リソース名, 0, 0, 0, 100)
		CALL ピクセル自動改行(デフォルトキャラ画像横幅)
		CALL 画像描画終了
	ELSE
		;PRINTFORML 存在しません
	ENDIF
ELSE
	;PRINTFORML 立ち絵追加リソース表示しません
ENDIF
