;※※※※※※※※※※※※※※※※※※※※※※※※※
;日常系コマンド
;※※※※※※※※※※※※※※※※※※※※※※※※※
;==================================================
;300,会話
;==================================================
@M_KOJO_MESSAGE_COM_K50_300
;コマンド共通メッセージ(TRAIN_MESSAGE) 
;削除すると「～話をした。」とかが消えます
CALL TRAIN_MESSAGE
;口上メイン呼出
CALL M_KOJO_MESSAGE_COM_K50_300_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_300_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;マイナスになってもいいさと考えるんだ
	;IF !CFLAG:1502 && CFLAG:信頼度 >= 500 && ABL:教養 > 2 && MONEY >= 300000
	;IF !CFLAG:1502 && CFLAG:信頼度 >= 500 && ABL:教養 > 2
	;	TRYCALLFORM M_KOJO_MESSAGE_COM_K50_進学
	;	RETURN 1
	;ENDIF
	
	IF 家庭菜園可(CFLAG:MASTER:現在位置) && !GETBIT (TCVAR:350,1)
		SETBIT TCVAR:350,1
		;SELECTCASE PLANT_TYPE_0
		SELECTCASE PLANT_TYPE
			CASE 151
				PRINTFORMW 「あ、キャベツ畑。」
				PRINTFORML 「家庭菜園をこんなにしちゃうなんて%UNICODE(0x2661)%」
				PRINTFORMW 「お兄様、赤ちゃん欲しいの？」
				IF CFLAG:生解禁 <= 2 && TALENT:恋人
					IF CFLAG:16:信頼度 < 500
						PRINTFORML 「でもお姉様が駄目だって」
					ELSE
						PRINTFORML 
						PRINTFORML 「いいよ」
						PRINTFORMW 「だって%CALLNAME:TARGET%もお兄様の赤ちゃん欲しいもの」
						PRINTFORMW 「うふふっ！ほらお兄様！キャベツ畑で捕まえてっ！」
						CFLAG:生解禁 = 2
					ENDIF
				ELSE
					PRINTDATAL
						DATALIST
							DATAFORM 「%CALLNAME:TARGET%にはまだ早いと思うの」
							;ばっちゃに語呂を合わせぱっちぇ呼称
							DATAFORM 「お赤飯が来ないと無理なんだってぱっちぇが言ってたもん」
						ENDLIST
						DATALIST
							DATAFORM 「お兄様、おあずけ」
							DATAFORM 「むりやりはやぁよ？うふふ♪」
						ENDLIST
						DATALIST
							DATAFORM 「欲・し・い・の・よ・ね？お・兄・様？」
							DATAFORM 「%CALLNAME:TARGET%にはお見通しなんだから」
							DATAFORM 「うふふ。まだだぁめ%UNICODE(0x2661)%」
						ENDLIST
					ENDDATA
				ENDIF
			CASE 163
				PRINTFORML 「お姉様も昔は青かったのかしら」
			CASE 164
				PRINTFORML 「スイカさん～スイカさん～おぉきくなぁれースイカさん～」
				PRINTFORML 「スイカさんみんなで食べようね！」
				PRINTFORMDW %CALLNAME:TARGET%はにぱっと笑った
			CASE 177
				PRINTFORML 「この葡萄が取れたらワインを作りましょ」
				PRINTFORML 「%CALLNAME:TARGET%ね。葡萄踏みしてみたかったの」
				PRINTFORML 「うふふ。楽しみね♪」
				PRINTFORMD %CALLNAME:TARGET%はスカートをたくし上げ
				
				IF TALENT:MASTER:マゾ
					PRINTFORMDL ると、%CALLNAME:MASTER%の股間に視線を送りながら足踏みをする仕草をした
				ELSE
					PRINTFORMDL ながらはしゃいでいる
				ENDIF
			CASE 187
				PRINTFORML 「ゆり～♪ゆりゆり～♪」
				PRINTFORML 「ねえねえお兄様。百合の根っこは食べられるんだよ！」
				SIF TALENT:MASTER:性別 == 3
					PRINTFORMDL %CALLNAME:TARGET%の%CALLNAME:MASTER%の股間を見る目がおかしい…
			CASE 188
				IF TCVAR:媚薬
					PRINTFORML 「ねえねえお兄様」
					PRINTFORML 「にんじんにレモンをかけると媚薬になるんだって！でも媚薬ってなぁに？」
				ELSE
					PRINTFORML 「にんじんさん♪だいこんさん♪しいたけさん♪ごぼうさん♪あな～の空いたぁれんこんさん♪」
					PRINTFORML 「すじーの通ったふぅーっきっ♪」
				ENDIF
		ENDSELECT
	ENDIF
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	
	IF TFLAG:193 == 1
		PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の言葉にうんうんと大きく頷いている
		RETURN 1
	ELSEIF TFLAG:193 == -1
		PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の言葉に小さく首を傾げている
		RETURN 1
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;ここに口上書いて下さい

ENDIF
RETURN 1
;==================================================
;301,お茶を淹れる
;==================================================
@M_KOJO_MESSAGE_COM_K50_301
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_301_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_301_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	
	IF TFLAG:193 == -1
		PRINTFORMW 「熱っ！」
		PRINTFORMDW %CALLNAME:TARGET%の舌には熱かったらしい
		PRINTFORMW 「舌がひりひりする」
		RETURN 1
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;302,スキンシップ
;==================================================
@M_KOJO_MESSAGE_COM_K50_302
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_302_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_302_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	SELECTCASE CFLAG:300
		CASE 13,24,39
			PRINTFORML 「おーふろできゅっきゅっきゅーっ♪」
			PRINTFORML 「ぶんぶんしーんぶん♪」
	ENDSELECT
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:10
			PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の手を取るとくるりと一回転した
			PRINTFORML 「お兄様」
			PRINTFORMDW %CALLNAME:TARGET%の真剣な眼差しが%CALLNAME:MASTER%を貫く！
			PRINTFORMDW 気付けば%CALLNAME:TARGET%の腕が%CALLNAME:MASTER%の腰を抱いている…
			PRINTFORML 「ふふっ」
			PRINTFORMDW 不意打ちに見せる%CALLNAME:TARGET%の高貴な側面に%CALLNAME:MASTER%の鼓動は高なった…
			PRINTFORML 
			PRINTFORML 「ダンスはいっつもお姉様が淑女役で%CALLNAME:TARGET%は紳士役ばっかりなの」
			PRINTFORMW 「お姉様ばっかりずるい」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;303,謝る
;==================================================
@M_KOJO_MESSAGE_COM_K50_303
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_303_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_303_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF TALENT:TARGET:2 & 2
		PRINTFORMW 「え？今何でもするって言った？」
		PRINTFORML 「お兄様」
		PRINTFORML 「いま何でもするって言ったわよね？うふふ%UNICODE(0x2661)%」
		CFLAG:MASTER:うふふ = 2
		CFLAG:うふふ = 2
		RETURN 1
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;304,仕事を手伝う
;==================================================
@M_KOJO_MESSAGE_COM_K50_304
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_304_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_304_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;ちんこついてる
	PRINTFORML 「ねこさんかわいいね！」
	IF TALENT:TARGET:2 & 2
		PRINTFORMW 「でも%CALLNAME:TARGET%はお兄様の『ねこさん』にもミルクあげたいなっ♪」
		PRINTFORMW %CALLNAME:MASTER%は部屋の奥に引き込まれ押し倒された
		CFLAG:MASTER:うふふ = 2
		CFLAG:うふふ = 2
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;305,膝枕してもらう
;==================================================
@M_KOJO_MESSAGE_COM_K50_305
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_305_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_305_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;306,お腹を撫でる
;==================================================
@M_KOJO_MESSAGE_COM_K50_306
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_306_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_306_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;307,頬をつまむ
;==================================================
@M_KOJO_MESSAGE_COM_K50_307
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_307_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_307_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;308,頬をつつく
;==================================================
@M_KOJO_MESSAGE_COM_K50_308
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_308_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_308_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	;IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	PRINTFORML 「ふにゅっ！」
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	;ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;309,頭を撫でる
;==================================================
@M_KOJO_MESSAGE_COM_K50_309
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_309_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_309_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF FIRSTTIME(SELECTCOM)
		PRINTFORML 「お兄様！フランはレディなんだから子供扱いしちゃ駄目なんだよ！」
		PRINTFORMDW フランに怒られてしまった……
	;恋慕 後期
	ELSEIF TALENT:恋慕
		IF !RAND:3
			PRINTFORML 「はふぅ……」
			PRINTFORMDW %CALLNAME:TARGET%の羽根はくてっとしている……
		ELSEIF !RAND:2
			PRINTFORML 「お兄様……」
		ELSE
			PRINTFORMW 「おにいさまになでなでされちゃった」
			PRINTFORMW 「むりやりされちゃった」
			PRINTFORMW 「ひどいよぉ……%UNICODE(0x2661)%」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			PRINTFORML 「むりやりなでなでだめぇ！」
		ELSEIF !RAND:2
			PRINTFORML 「お兄様ぁ……」
			PRINTFORML 「だめだよぉ……だめだよぉ……」
			PRINTFORMDW %CALLNAME:TARGET%は涙ぐんでいる……
		ELSE
			PRINTFORML 「お兄様なでなでしちゃだめぇ…」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			PRINTFORML 「やぁ！」
			PRINTFORMDW %CALLNAME:TARGET%はカリスマ溢れる構えで%CALLNAME:MASTER%の手のひらを防いでいる
		ELSEIF !RAND:2
			PRINTFORML 「やだよぉ……たらこ来ちゃうよぉ……」
		ELSE
			PRINTFORML 「だめだよぉ…だめだよぉ…」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;310,尻を撫でる
;==================================================
@M_KOJO_MESSAGE_COM_K50_310
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_310_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_310_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;311,抱き付く
;==================================================
@M_KOJO_MESSAGE_COM_K50_311
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_311_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_311_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	PRINTFORML 「えへへー♪むぎゅむぎゅー♪」
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;312,キスする
;==================================================
@M_KOJO_MESSAGE_COM_K50_312
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_312_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_312_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	IF FIRSTTIME()
		PRINTFORML 「お兄様。」
		PRINTFORMDW %CALLNAME:TARGET%は真剣な面持ちで%CALLNAME:MASTER%を捉えた
		PRINTFORMW 「ちゅーすると赤ちゃんができちゃう事があるのよ」
		PRINTFORMW 「簡単にしちゃ駄目なんだよ？」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			PRINTFORML 「お兄様。%CALLNAME:TARGET%のおちんちんがちゅーして%UNICODE(0x2661)%って言ってるの」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;妊娠中
	IF TALENT:妊娠
	;	PRINTFORML 「」
		RETURN 1
	ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			PRINTFORML 「ふぁ…%UNICODE(0x2661)%」
			PRINTFORMW 「お兄様ぁ…%CALLNAME:TARGET%、赤ちゃんできちゃうよぉ…%UNICODE(0x2661)%」
		;ELSEIF !RAND:2
		ELSE
			PRINTFORMW 「%CALLNAME:TARGET%。%CALLNAME:TARGET%ママになっちゃうよぉ…%UNICODE(0x2661)%」
		ENDIF
		RETURN 1
	;思慕 中期
	;ELSEIF TALENT:思慕
	;	IF !RAND:3
	;		;PRINTFORML 「」
	;	ELSEIF !RAND:2
	;		;PRINTFORML 「」
	;	ELSE
	;		;PRINTFORML 「」
	;	ENDIF
	;	RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			PRINTFORML 「お兄様ぁ…ちゅっちゅしちゃだめだよぉ…」
		ELSEIF !RAND:2
			PRINTFORMW 「%CALLNAME:TARGET%。%CALLNAME:TARGET%ママになっちゃうよぉ…」
		ELSE
			PRINTFORMW 「ちゅっちゅしたらできちゃう…赤ちゃんできちゃうよぉ…」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;313,胸愛撫
;==================================================
@M_KOJO_MESSAGE_COM_K50_313
;CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_313_1
CALL TRAIN_MESSAGE
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_313_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	IF TIME > 360 && TIME > 1020
		PRINTFORML 「お兄様。おてんとさまに見られちゃうよ？いけないんだよ？」
		PRINTFORMDW %CALLNAME:TARGET%はえっちな事は夜まで待って欲しいと言ってきた。
		PRINTFORML 
		PRINTFORML 「ふぇぇ駄目だよぉ」
		PRINTFORML 「駄目だよぉ」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;314,アナル愛撫
;==================================================
@M_KOJO_MESSAGE_COM_K50_314
CALL TRAIN_MESSAGE
;CALL M_KOJO_MESSAGE_COM_K50_314_1
CALL M_KOJO_MESSAGE_COM_K50_313_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_314_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;315,クリ愛撫
;==================================================
@M_KOJO_MESSAGE_COM_K50_315
CALL TRAIN_MESSAGE
;CALL M_KOJO_MESSAGE_COM_K50_315_1
CALL M_KOJO_MESSAGE_COM_K50_313_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_315_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;316,指挿入れ
;==================================================
@M_KOJO_MESSAGE_COM_K50_316
CALL TRAIN_MESSAGE
;CALL M_KOJO_MESSAGE_COM_K50_316_1
CALL M_KOJO_MESSAGE_COM_K50_313_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_316_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;317,Ｖ腰を振る
;==================================================
@M_KOJO_MESSAGE_COM_K50_317
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_317_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_317_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;318,Ａ腰を振る
;==================================================
;@M_KOJO_MESSAGE_COM_K50_318
;CALL TRAIN_MESSAGE
;CALL M_KOJO_MESSAGE_COM_K50_318_1
;RETURN RESULT

;@M_KOJO_MESSAGE_COM_K50_318_1
;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
;LOCAL = 0
;-------------------------------------------------
;IF LOCAL

;ENDIF
;RETURN 1
;==================================================
;330,スカートめくり
;==================================================
@M_KOJO_MESSAGE_COM_K50_330
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_330_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_330_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF FIRSTTIME(SELECTCOM)
	;幼女のマジ泣き。可愛げを出さないように
		PRINTFORMW 「え……」
		PRINTFORMDW %CALLNAME:TARGET%の呆気にとられた表情がみるみるうちに崩れていく…
		PRINTFORMW 「うえぇ……」
		PRINTFORML 「う”あ”ぁ”ーー！」
		PRINTFORMW 「お”に”い”さ”ま”がいじめた”ーー！！」
		PRINTFORMW 「あ”ぁ”ーー！！あ”ーーー！！！」
	;犯される生娘のように真剣に
	ELSE
		PRINTFORMW 「やめてよ！」
		PRINTFORMW 「お兄様やめて！」
		PRINTFORMW 「やめてお兄様やめて！」
		PRINTFORMW 「やめてよぉ……」
		PRINTFORMDW %CALLNAME:TARGET%の頬を大粒の雫が伝う…
		RETURN 1
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;331,お茶を淹れる（薬入り）
;==================================================
@M_KOJO_MESSAGE_COM_K50_331
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_331_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_331_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF FIRSTTIME(SELECTCOM)
		PRINTFORML 「この味、%CALLNAME:15%の味ね」
		PRINTFORML 「上手にできたのねお兄様」
		PRINTFORML 「えらいえらい」
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;350,押し倒す（時姦時イタズラする
;TFLAG:194 (0=成功 1=人目がある　2=野外は嫌でござりまする　3=多人数プレイは勘弁　4=合意があってもムードが足りぬ）
;TCVAR:抱き寄せ初回　1=普通に合意ゲット　2=先にキスぐらいしろよ　3=媚薬or発情には勝てなかったよお…　4=悔しい…刻印さえなければﾋﾞｸﾋﾞｸﾝ
;5=強引に迫られると断れぬ(素質臆病持ち専用) 6=まんざらでもない　7=駄目　8=論外
;==================================================
@M_KOJO_MESSAGE_COM_K50_350
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_350_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_350_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;351,連れ出す
;==================================================
@M_KOJO_MESSAGE_COM_K50_351
;連れ出しにはTRAIN_MESSAGEが無い
CALL M_KOJO_MESSAGE_COM_K50_351_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_351_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF FIRSTTIME()
		PRINTFORML 「どうしたのお兄様？」
		PRINTFORML 「……ひょっとして」
		PRINTFORML 「ひとりでおトイレに行けないの？」
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;352,告白する
;==================================================
@M_KOJO_MESSAGE_COM_K50_352
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_352_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_352_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;353,イタズラする
;==================================================
@M_KOJO_MESSAGE_COM_K50_353
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_353_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_353_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;354,添い寝する（相手は起きて布団に招く感じ）開始側
;==================================================
@M_KOJO_MESSAGE_COM_K50_354
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_354_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_354_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;355,時止め（地の文の改変程度に留めるべき
;==================================================
@M_KOJO_MESSAGE_COM_K50_355
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_355_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_355_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;360,身を任せる
;==================================================
@M_KOJO_MESSAGE_COM_K50_360
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_360_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_360_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;363,読み聞かせ（[幼稚]持ち）
;==================================================
@M_KOJO_MESSAGE_COM_K50_363
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_363_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_363_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;364,豆まき(萃香、勇儀、スカーレット姉妹用
;==================================================
@M_KOJO_MESSAGE_COM_K50_364
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_364_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_364_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;365,守矢神社へ移動(早苗、神奈子、諏訪子用
;==================================================
@M_KOJO_MESSAGE_COM_K50_365
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_365_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_365_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;402,就寝（添い寝時）
;==================================================
@M_KOJO_MESSAGE_COM_K50_402
;就寝にTRAIN_MESSAGEは無い
CALL M_KOJO_MESSAGE_COM_K50_402_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_402_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;403,休憩
;==================================================
@M_KOJO_MESSAGE_COM_K50_403
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_403_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_403_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;405,出掛ける(同行時デートに出掛ける
;==================================================
@M_KOJO_MESSAGE_COM_K50_405
;出掛けるにTRAIN_MESSAGEは無い
CALL M_KOJO_MESSAGE_COM_K50_405_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_405_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;410,掃除
;==================================================
@M_KOJO_MESSAGE_COM_K50_410
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_410_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_410_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;411,戦闘訓練
;==================================================
@M_KOJO_MESSAGE_COM_K50_411
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_411_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_411_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;412,勉強
;==================================================
@M_KOJO_MESSAGE_COM_K50_412
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_412_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_412_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;413,料理
;==================================================
@M_KOJO_MESSAGE_COM_K50_413
;料理にTRAIN_MESSAGEは無い
CALL M_KOJO_MESSAGE_COM_K50_413_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_413_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;初めて
	;記入チェック
	LOCAL:1 = 1
	;-------------------------------------------------
	IF LOCAL:1 && FIRSTTIME(SELECTCOM)
		;吸血鬼の性知識
		PRINTFORML 「痛っ！」
		PRINTFORMDW 料理中、%CALLNAME:TARGET%は指を切ってしまった
		PRINTFORML 
		PRINTFORMDW %CALLNAME:MASTER%が傷口を吸うと%CALLNAME:TARGET%はあたふたと慌てだした
		PRINTFORML 「お、お兄様？」
		PRINTFORMW 「%CALLNAME:TARGET%は吸血鬼だからその、吸血は、繁殖行為なんだよ？」
		PRINTFORMW 「やだどうしよう。%CALLNAME:TARGET%嫁入り前なのに、お兄様に繁殖されちゃった」
		PRINTFORML 「お姉様にだってされたことないのにされたことないのに」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;414,食事
;==================================================
@M_KOJO_MESSAGE_COM_K50_414
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_414_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_414_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	IF TASTE_REACTION("辛い")
		PRINTFORMDL スカーレットカラーの料理に%CALLNAME:TARGET%は冷や汗を流している…
		PRINTFORMW 「ね、ねぇ、お兄様」
		PRINTFORMW 「辛そうね？」
		PRINTFORMW 「たらこじゃないよね？」
		PRINTFORMDW あなたがたらこじゃない事を確約すると%CALLNAME:TARGET%は恐る恐ると料理を口に運んだ…
		PRINTFORML ぱくっ！
		PRINTFORML 「からあぃ！！」
		;時間停止イラマチオのススメ
		PRINTFORMDL %CALLNAME:TARGET%はんべー！っと舌を突き出した…
		RETURN 1
	ENDIF
	
	IF TASTE_REACTION("わさび")
;		PRINTFORMDW %CALLNAME:MASTER%は妊娠している%CALLNAME:TARGET%に酸っぱい物を与えることにした…
		PRINTFORML 「ん～～っ！」
		PRINTFORML  「ごつごつしたわさびさんが%CALLNAME:TARGET%のお鼻の奥をつんつんってしてるよぉ！」
		RETURN 1
	ENDIF
	;妊娠中
	IF TALENT:妊娠
		IF TASTE_REACTION("すっぱい")
			PRINTFORMDW %CALLNAME:MASTER%は妊娠している%CALLNAME:TARGET%に酸っぱい物を与えることにした…
			PRINTFORML 「%CALLNAME:TARGET%の好きな味付け？」
			PRINTFORML  「あっ！すっぱくて美味しい♪」
			SIF TALENT:処女 == -1
				PRINTFORMD 自分が身籠っていると知らない彼女のカラダは自ずと柑橘を求め、
			PRINTFORML %CALLNAME:TARGET%は幸せそうな表情で%CALLNAME:MASTER%の手料理を頬張っている…
		ENDIF
		RETURN 1
	ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;415,食事をふるまう
;==================================================
@M_KOJO_MESSAGE_COM_K50_415
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_415_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_415_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	IF TASTE_REACTION("辛い")
		PRINTFORMDL スカーレットカラーの料理に%CALLNAME:TARGET%は冷や汗を流している…
		PRINTFORMW 「ね、ねぇ、お兄様」
		PRINTFORMW 「辛そうね？」
		PRINTFORMW 「たらこじゃないよね？」
		PRINTFORMDW あなたがたらこじゃない事を確約すると%CALLNAME:TARGET%は恐る恐ると料理を口に運んだ…
		PRINTFORML ぱくっ！
		PRINTFORML 「からあぃ！！」
		;時間停止イラマチオのススメ
		PRINTFORMDL %CALLNAME:TARGET%はんべー！っと舌を突き出した…
		RETURN 1
	ENDIF
	
	IF TASTE_REACTION("甘い")
		IF TALENT:恋人
			PRINTFORML （この味…）
			PRINTFORMDW %CALLNAME:TARGET%は味付けのメッセージを受け取ると%CALLNAME:MASTER%の耳元で小さく囁いた…
			PRINTFORML 「いいよ♪二人きりになったら一杯あまあま%UNICODE(0x2661)%しましょ」
		ELSE
			PRINTFORMDW 「ごはんあまいね！」
		ENDIF
		RETURN 1
	ENDIF
	IF TASTE_REACTION("わさび")
;		PRINTFORMDW %CALLNAME:MASTER%は妊娠している%CALLNAME:TARGET%に酸っぱい物を与えることにした…
		PRINTFORML 「ん～～っ！」
		PRINTFORML  「ごつごつしたわさびさんが%CALLNAME:TARGET%のお鼻の奥をつんつんってしてるぅ！」
		RETURN 1
	ENDIF
	;妊娠中
	IF TALENT:妊娠
		IF TASTE_REACTION("すっぱい")
			PRINTFORMDW %CALLNAME:MASTER%は妊娠している%CALLNAME:TARGET%に酸っぱい物を与えることにした…
			PRINTFORML 「%CALLNAME:TARGET%の好きな味付け？」
			PRINTFORML  「あっ！すっぱくて美味しい♪」
			SIF TALENT:処女 == -1
				PRINTFORMD 自分が身籠っていると知らない彼女のカラダは自ずと柑橘を求め、
			PRINTFORML %CALLNAME:TARGET%は幸せそうな表情で%CALLNAME:MASTER%の手料理を頬張っている…
		ENDIF
		RETURN 1
	ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;416,演奏する(凍結中？
;==================================================
@M_KOJO_MESSAGE_COM_K50_416
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_416_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_416_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;417,昼寝をする
;==================================================
@M_KOJO_MESSAGE_COM_K50_417
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_417_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_417_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;421,願掛けする
;==================================================
@M_KOJO_MESSAGE_COM_K50_421
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_421_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_421_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;431,お風呂でくつろぐ
;==================================================
@M_KOJO_MESSAGE_COM_K50_431
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_431_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_431_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF TALENT:機嫌 == -1
		;IF !RAND:3
			PRINTFORMDL %CALLNAME:TARGET%は湯船に口まで浸かって泡を立てている…
		;ELSEIF TALENT:機嫌 == -1 && !RAND:2
			;躾がなっていない事と幼い事は別物故に破棄
		;	;PRINTFORMDL %CALLNAME:TARGET%は湯船を掴み足をばたつかせている…
		;何もしない分岐
		;ELSE
		;ENDIF
		RETURN 1
	ENDIF
	
	IF !RAND:10
		PRINTFORML 「お兄様！お兄様！アヒルちゃんだッ！」
		PRINTFORML 「うり～っ！！」
		PRINTFORMDW %CALLNAME:TARGET%はぐりぐりとアヒールちゃーんを押し付けてきた…
	ELSEIF !RAND:9
		PRINTFORMDW %CALLNAME:TARGET%は手拭いで球を作っては沈め、作っては沈めている…
		PRINTFORML 「ぶくぶく～っ！」
	ELSEIF !RAND:8
		PRINTFORMW 「お兄様お兄様！%CALLNAME:TARGET%はね。お湯に浸かるとね」
		PRINTFORML 「増えるんだよ！」
		PRINTFORMW 『増えるんだよ！』
		PRINTFORML 「頭を洗ってる時に後ろを見張っててくれるんだよ！」
		PRINTFORMW 『頭を洗ってる時に後ろを見張っててくれるんだよ！』
		PRINTFORML 「ねー？」
		PRINTFORMW 『ねー？』
	ELSEIF !RAND:7
		
	ELSE
	ENDIF
	
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;440,待つ
;==================================================
@M_KOJO_MESSAGE_COM_K50_440
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_440_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_440_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF !RAND:3
		PRINTFORML 「あーぶくたったーにえたったー♪にえたかどうだかたべてみよー」
		PRINTFORML 「むしゃむしゃ」
		PRINTFORML 「まだ煮えない」
		PRINTFORMDW %CALLNAME:TARGET%は%CALLNAME:MASTER%の傍で暇をつぶしている
	ELSEIF !RAND:2
		PRINTFORML 「ねーおにいさま？」
		PRINTFORMW 「………」
		PRINTFORML 
		PRINTFORMW 「レディを退屈させるものではないわ」
		PRINTFORML 「キリッ！」
	ELSE
		PRINTFORML 「ねーねーおにいさまっ。」
		PRINTFORMW 「あそびましょっ？」
		PRINTFORMW %CALLNAME:TARGET%はくいくいとあなたの腕を引っ張ってきた
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;441,授業を受ける(慧音専用？
;==================================================
@M_KOJO_MESSAGE_COM_K50_441
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_441_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_441_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	;IF CFLAG:うふふ == 2
	;	;ちんこついてる
	;	IF TALENT:TARGET:2 & 2
	;		PRINTFORML 「せんせーい！バナナが入りました～！」
	;		PRINTFORMDW %CALLNAME:TARGET%のバナナに貫かれながら%CALLNAME:MASTER%は獣のような声を上げている…
	;	;ちんこついてない
	;	ELSE
	;		PRINTFORML 「せんせーい！バナナはおやつに入りますか？」
	;		PRINTFORMDW %CALLNAME:TARGET%はここぞとばかりに質問した
	;	ENDIF
	;	RETURN 1
	;ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;442,伐採する
;==================================================
@M_KOJO_MESSAGE_COM_K50_442
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_442_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_442_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;※※※※※※※※※※※※※※※※※※※※※※※※※
;デート系コマンド
;※※※※※※※※※※※※※※※※※※※※※※※※※
[SKIPSTART]
;現状未実装
;==================================================
;600,デート会話
;==================================================
@M_KOJO_MESSAGE_COM_K50_600
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_600_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_600_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;601,デートスキンシップ
;==================================================
@M_KOJO_MESSAGE_COM_K50_601
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_601_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_601_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;602,デートキス
;==================================================
@M_KOJO_MESSAGE_COM_K50_602
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_602_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_602_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
[SKIPEND]
;==================================================
;603,手を繋ぐ
;==================================================
@M_KOJO_MESSAGE_COM_K50_603
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_603_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_603_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;604,散策する
;==================================================
@M_KOJO_MESSAGE_COM_K50_604
;散策するにはTRAIN_MESSAGEが無い
CALL M_KOJO_MESSAGE_COM_K50_604_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_604_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;605,寄り道する
;==================================================
@M_KOJO_MESSAGE_COM_K50_605
;寄り道するにはTRAIN_MESSAGEが無い
CALL M_KOJO_MESSAGE_COM_K50_605_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_605_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF !RAND:5
		PRINTFORMDW %CALLNAME:TARGET%はもじもじしながら%CALLNAME:MASTER%の服の裾を掴みながら上目遣いで訴えてきた
		PRINTFORMDW %CALLNAME:TARGET%の瞳は涙で潤んでいる…
		PRINTFORMW 「……おしっこ」
	ELSEIF !RAND:4
	ELSEIF !RAND:3
		PRINTFORMDW 理性を失った%CALLNAME:MASTER%は%CALLNAME:TARGET%を人気のない駄菓子屋に引きずり込むと%CALLNAME:TARGET%の口に黒く野太い麩菓子を捻じ込んだ！
		MONEY:0 -= 20
	ELSEIF !RAND:2
		IF !RAND:2
			PRINTFORMDW %CALLNAME:MASTER%はよいこの%CALLNAME:TARGET%ちゃんに綿菓子を買ってあげた
			MONEY:0 -= 500
		ELSE
			PRINTFORMDW %CALLNAME:MASTER%はよいこの%CALLNAME:TARGET%ちゃんにペロペロキャンディを買ってあげた
			MONEY:0 -= 500
		ENDIF
	ELSE
		PRINTFORMDW しまっちゃうおじさんは%CALLNAME:MASTER%と%CALLNAME:TARGET%の行く末を愛情の籠った目で眺めている
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;610,食事に誘う
;==================================================
@M_KOJO_MESSAGE_COM_K50_610
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_610_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_610_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;613,お金稼ぎする
;==================================================
@M_KOJO_MESSAGE_COM_K50_613
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_613_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_613_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;614,食材の買い出し
;==================================================
@M_KOJO_MESSAGE_COM_K50_614
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_614_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_614_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;615,お弁当を食べる
;==================================================
@M_KOJO_MESSAGE_COM_K50_615
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_615_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_615_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	IF !TALENT:処女 && !RAND:(ABL:料理技能 + 1)
		PRINTFORMW 「ねぇねぇお兄様。%CALLNAME:TARGET%いいお嫁さんになれるかな？」
		PRINTFORMW 「もうお嫁に行けないの？そんな事言ってもお兄様」
		PRINTFORMW 「それはお姉様が決めるのよ」
	ELSE
	PRINTDATAL
		DATALIST
			DATAFORM 「おっべんとおっべんとうっれしっいなー♪」
			DATAFORM 「たっすけったかっめにーつーれらーれてー♪」
		ENDLIST
		DATALIST
			DATAFORM 「お兄様お兄様！%CALLNAME:TARGET%いいお嫁さんになれるかな？なれるかな？」
			DATAFORM 「なれるよね！うん。フラン頑張る！」
		ENDLIST
		DATALIST
			DATAFORM 「お兄様、片面だけ焼いた目玉焼きはサニーサイドアップなんだよ」
			DATAFORM 「フランはおてんとさまは苦手。だからムーンサイドアップね」
			DATAFORM フランは束ねた髪をふりふりしている
		ENDLIST
	ENDDATA
	ENDIF
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;617,甘味処に誘う
;==================================================
@M_KOJO_MESSAGE_COM_K50_617
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_617_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_617_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	PRINTFORML 「お兄様！おいしいねっ！」
	PRINTFORML 「カリスマも下り鰻だねっ！」
	PRINTFORML 「%UNICODE(0x2661)%」
	PRINTFORMDW %CALLNAME:TARGET%は羽根をピコピコ羽ばたかせながら甘味を頬張っている
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;618,花屋
;==================================================
@M_KOJO_MESSAGE_COM_K50_618
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_618_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_618_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	PRINTFORML 「綺麗」
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;620,鈴奈庵
;==================================================
@M_KOJO_MESSAGE_COM_K50_620
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_620_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_620_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	IF TALENT:妊娠
		PRINTFORML 「お兄様。推薦図書はたまぴよくらぶよ。らぶらぶよ。」
	ELSE
		PRINTFORMDL %CALLNAME:TARGET%は物凄く期待している…
	ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;660,読書
;==================================================
@M_KOJO_MESSAGE_COM_K50_660
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_660_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_660_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	IF EQUIP:アクセサリ == 1
		PRINTFORMDL 眼鏡をかけ読書に耽る%CALLNAME:TARGET%の佇まいは中々に様になっており、
		PRINTFORMDW 無邪気な挙動に普段は影を潜めている彼女の中の気品を存分に引き出していた
		PRINTFORML 「…何かしらお兄様」
		IF TALENT:TARGET:2 & 2
			PRINTFORMDW %CALLNAME:TARGET%はわるいこのえほんをそっと閉じた…
		ELSEIF ABL:教養 < 3
			PRINTFORMDW %CALLNAME:TARGET%はよいこのえほんをそっと閉じた…
		ENDIF
	ELSE
		PRINTFORMDW いつの間にか%CALLNAME:TARGET%は眼鏡をかけていた
		EQUIP:アクセサリ = 1
	ENDIF
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;698,部屋に入る
;==================================================
@M_KOJO_MESSAGE_COM_K50_698
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_698_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_698_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;699,外に出る
;==================================================
@M_KOJO_MESSAGE_COM_K50_699
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_699_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_699_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;※※※※※※※※※※※※※※※※※※※※※※※※※
;派生コマンド
;※※※※※※※※※※※※※※※※※※※※※※※※※
;==================================================
;60,耳かき
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_60
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_SCOM_K50_60_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_60_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;61,抱き枕
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_61
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_SCOM_K50_61_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_61_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		;IF !RAND:3
			;PRINTFORML 「」
		;ELSEIF !RAND:2
			;PRINTFORML 「」
		;ELSE
			;PRINTFORML 「」
		;ENDIF
		PRINTFORML 「んきゅぅ…」
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;62,デコちゅー
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_62
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_SCOM_K50_62_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_62_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;63,もっとキスする
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_63
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_SCOM_K50_63_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_63_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	IF TALENT:妊娠
	;	PRINTFORML 「」
		RETURN 1
	ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	PRINTFORML （お兄様ったら、本当に%CALLNAME:TARGET%を孕ませたいんだ…）
	;恋慕 後期
	IF TALENT:恋慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:思慕
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;80,手を引く
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_80
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_SCOM_K50_80_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_80_1

;-------------------------------------------------
;記入チェック（=0, 非表示、1, 表示）
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;時間停止中は無言 喋らせる際は;を消して「台詞」を加筆の事
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;睡眠中
	IF CFLAG:睡眠 == 1
		RETURN 1
	ENDIF
	;時間停止以外でシラフでない場合
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;相手主導押し倒され中
	IF CFLAG:うふふ == 2
		;ちんこついてる
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;ちんこついてない
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;あなた主導押し倒し中
	IF CFLAG:うふふ == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;特殊分岐 使用時にはコメントアウトを解放してください
	;-------------------------------------------------
	;;妊娠中
	;IF TALENT:妊娠
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;精液ボテ
	;IF !TALENT:妊娠 && EX:TARGET:膣内精液 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;以下日常 とりあえずランダム三枠
	;-------------------------------------------------
	;恋慕 後期
	IF TALENT:16:恋慕
		IF !RAND:3
			PRINTFORML 「お姉様を悲しませたら%CALLNAME:TARGET%駄目なの」
			PRINTFORMDW %CALLNAME:TARGET%は目に涙を溜め訴えてきた
		ELSEIF !RAND:2
			PRINTFORML 「駄目なのお兄様！」
		ELSE
			PRINTFORML 「おにいさまだめぇ…」
		ENDIF
		RETURN 1
	;思慕 中期
	ELSEIF TALENT:16:思慕
		IF !RAND:3
			PRINTFORML 「お姉様に見られたらヤキモチされちゃうわね」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;それ以外
	ELSE
		IF !RAND:3
			PRINTFORML 「待ってお兄様！%CALLNAME:TARGET%はじぽきんで違法なのよ！」
		ELSEIF !RAND:2
			PRINTFORML 「お兄様！待って！待って！」
		ELSE
			PRINTFORML 「引っ張っちゃだめだよぉ…お兄様ぁ…」
			PRINTFORMW 「お兄様捕まって提訴されちゃうよぉ…」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1