@EXIST_酒データ1
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "ただの水")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "どぶろく")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT


@EXIST_酒データ2
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ2(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "霧の湖の水")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "清酒")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ3
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ3(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "地獄温泉の水")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "鬼殺し")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ4
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ4(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "ブドウ汁")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "ワイン")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ5
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ5(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "奇跡の水")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "お神酒")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ6
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ6(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "米こうじ")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "甘酒")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 1)
CASE "醸造数"
	LOCAL = MAX(1, RAND:(ABL:MASTER:教養 + 1))
	CALLF MAKE_INT(V_NAME, LOCAL)
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ10
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ10(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "イモ入り水")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "もろみ(芋)")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ11
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ11(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "ハチミツ水")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "ミード")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ12
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ12(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "キイチゴ水")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "フルーツワイン")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT
@EXIST_酒データ13
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ13(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "砂糖水")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "発酵した砂糖汁")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ14
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ14(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "熟成前フランボワーズ")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "フランボワーズ")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ15
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ15(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "熟成前ラム")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "ホワイトラム")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ16
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ16(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "ブドウ汁(ALC添加)")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "酒精強化ワイン")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ17
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ17(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "貴腐ブドウ汁")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "貴腐ワイン")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ18
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ18(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "熟成前ブランデー")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "ブランデーVS")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ19
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ19(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "発酵した砂糖汁")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
;CASE "醸造日数"
;	CALLF MAKE_INT(V_NAME, 5)
CASE "醸造数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ49
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ49(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "芋焼酎")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
CASE "醸造数"
	LOCAL = 0
	CALLF MAKE_INT(V_NAME,)
CASE "酒気増加量"
	LOCAL = 700 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:酒耐性 < 0
		LOCAL = 2
	ELSEIF TALENT:ARG:酒耐性 < 2
		LOCAL = 8
	ELSE
		LOCAL = 22
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ50
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ50(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "一夜のクシナダ")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
CASE "酒気増加量"
	LOCAL = 500 + RAND:200
	;一夜のクシナダは酒耐性が高いほど酔いが回りやすい
	IF ARG != MASTER
		IF TALENT:ARG:酒耐性 == -1
			TIMES LOCAL, 1.1
		ELSEIF TALENT:ARG:酒耐性 == 1
			TIMES LOCAL, 1.2
		ELSEIF TALENT:ARG:酒耐性 == 2
			TIMES LOCAL, 1.7
		ELSEIF TALENT:ARG:酒耐性 == 3
			TIMES LOCAL, 2.5
		ELSEIF TALENT:ARG:酒耐性 == -2
			IF CFLAG:ARG:下戸飲み >= 5
				TIMES LOCAL, 2.0
			ELSEIF CFLAG:ARG:下戸飲み >= 2
				TIMES LOCAL, 1.5
			ELSE
				TIMES LOCAL, 1.0
			ENDIF
		ENDIF
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:酒耐性 < 0
		LOCAL = 8
	ELSE
		LOCAL = 25
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT


@EXIST_酒データ51
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ51(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "どぶろく")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "清酒")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
CASE "醸造数"
	LOCAL = 10 + RAND:(ABL:MASTER:教養 * 2 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 300 + RAND:100
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
		IF TALENT:ARG:酒耐性 > 0
			LOCAL = 7
		ELSE
			LOCAL = 10
		ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ52
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ52(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "清酒")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 0)
CASE "醸造数"
	LOCAL = 10 + RAND:(ABL:MASTER:教養 * 2 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 400 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
		IF TALENT:ARG:酒耐性 < 0
			LOCAL = 8
		ELSE
			LOCAL = 15
		ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ53
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ53(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "鬼殺し")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 0)
CASE "醸造数"
	LOCAL = 5 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 300 + RAND:100
	SIF CHECK_CHARA(ARG, "鬼")
		LOCAL *= 3
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
		IF CHECK_CHARA(ARG, "鬼")
			LOCAL = 30
		ELSEIF TALENT:ARG:酒耐性 <= 0
			LOCAL = 5
		ELSE
			LOCAL = 12
		ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ54
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ54(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "ワイン")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "熟成ワイン")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
CASE "醸造数"
	LOCAL = 5
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 200 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
		IF TALENT:ARG:酒耐性 < 0
			LOCAL = 7
		ELSE
			LOCAL = 10
		ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ55
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ55(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "お神酒")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
CASE "醸造数"
	LOCAL = 1 + RAND:(ABL:MASTER:教養 / 2 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 300 + RAND:600
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:神霊
		LOCAL = 30
	ELSEIF TALENT:ARG:酒耐性 > 0
		LOCAL = 7
	ELSEIF TALENT:ARG:酒耐性 < 0
		LOCAL = 8
	ELSE
		LOCAL = 10
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ56
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ56(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "甘酒")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 1)
CASE "醸造数"
	LOCAL = 10 + RAND:(ABL:MASTER:教養 * 2 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 1
	SIF TALENT:ARG:酒耐性 <= -1
		LOCAL = 20 + RAND:30
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:体型 <= -1 || TALENT:ARG:幼児／幼児退行 || TALENT:ARG:幼稚
		LOCAL = 15
	ELSEIF TALENT:ARG:酒耐性 > 0
		LOCAL = 9
	ELSE
		LOCAL = 10
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EFFECT_酒データ56(ARG)
CALL RECOVER(MASTER, 50, "体力", "甘酒")
CALL RECOVER(MASTER, 80, "気力", "甘酒")
CALL RECOVER(ARG, 50, "体力", "甘酒")
CALL RECOVER(ARG, 80, "気力", "甘酒")

;57は欠番

@EXIST_酒データ58
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ58(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "蒸留酒")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
CASE "醸造数"
	CALLF MAKE_INT(V_NAME,)
CASE "酒気増加量"
	LOCAL = 700 + RAND:200
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:酒耐性 < 0
		LOCAL = 3
	ELSEIF TALENT:ARG:酒耐性 < 2
		LOCAL = 9
	ELSE
		LOCAL = 20
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ59
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ59(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "薬酒")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
CASE "醸造数"
	CALLF MAKE_INT(V_NAME, )
CASE "酒気増加量"
	LOCAL =  200 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:調合知識 || TALENT:ARG:薬毒耐性 || ABL:ARG:教養 >= 3
		LOCAL = 20
	ELSEIF TALENT:ARG:酒耐性 < 0
		LOCAL = 7
	ELSE
		LOCAL = 9
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EFFECT_酒データ59(ARG)
CALL RECOVER(MASTER, 150, "体力", "薬酒")
CALL RECOVER(MASTER, 200, "気力", "薬酒")
CALL RECOVER(ARG, 150, "体力", "薬酒")
CALL RECOVER(ARG, 200, "気力", "薬酒")

@EXIST_酒データ60
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ60(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "もろみ(芋)")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
CASE "醸造数"
	LOCAL = 5 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ61
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ61(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "ミード")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
CASE "醸造数"
	LOCAL = 5 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 200 + RAND:200
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:酒耐性 > 1
		LOCAL = 7
	ELSEIF TALENT:ARG:酒耐性 < 0
		LOCAL = 10
	ELSE
		LOCAL = 20
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ62
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ62(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "フルーツワイン")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
CASE "醸造数"
	LOCAL = 5 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 200 + RAND:200
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:酒耐性 > 1
		LOCAL = 8
	ELSEIF TALENT:ARG:酒耐性 < 0
		LOCAL = 9
	ELSE
		LOCAL = 20
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT


@EXIST_酒データ63
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ63(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "フランボワーズ")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
CASE "醸造数"
	LOCAL = 1
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 900 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:酒耐性 < 1
		LOCAL = 1
	ELSEIF TALENT:ARG:酒耐性 < 2
		LOCAL = 6
	ELSEIF TALENT:ARG:酒耐性 < 3
		LOCAL = 20
	ELSE
		LOCAL = 25
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ64
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ64(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "ホワイトラム")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "ゴールドラム")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
CASE "醸造数"
	LOCAL = 3
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 900 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:酒耐性 < 1
		LOCAL = 3
	ELSEIF TALENT:ARG:酒耐性 < 2
		LOCAL = 10
	ELSEIF TALENT:ARG:酒耐性 < 3
		LOCAL = 20
	ELSE
		LOCAL = 22
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ65
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ65(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "ゴールドラム")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "ダークラム")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
CASE "醸造数"
	LOCAL = 3
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 900 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:酒耐性 < 1
		LOCAL = 3
	ELSEIF TALENT:ARG:酒耐性 < 2
		LOCAL = 10
	ELSEIF TALENT:ARG:酒耐性 < 3
		LOCAL = 22
	ELSE
		LOCAL = 25
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ66
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ66(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "ダークラム")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
CASE "醸造数"
	LOCAL = 3
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 900 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:酒耐性 < 1
		LOCAL = 1
	ELSEIF TALENT:ARG:酒耐性 < 2
		LOCAL = 6
	ELSEIF TALENT:ARG:酒耐性 < 3
		LOCAL = 25
	ELSE
		LOCAL = 30
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ67
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ67(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "酒精強化ワイン")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
CASE "醸造数"
	LOCAL = 7 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 400 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:酒耐性 < 0
		LOCAL = 10
	ELSEIF TALENT:ARG:酒耐性 < 3
		LOCAL = 20
	ELSE
		LOCAL = 15
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ68
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ68(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "貴腐ワイン")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
CASE "醸造数"
	LOCAL =  5 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 400 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF ABL:ARG:教養 >= 3
		LOCAL = 25
	ELSEIF TALENT:ARG:酒耐性 < -1
		LOCAL = 10
	ELSEIF TALENT:ARG:酒耐性 < 0
		LOCAL = 15
	ELSEIF TALENT:ARG:酒耐性 < 3
		LOCAL = 25
	ELSE
		LOCAL = 20
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT


@EXIST_酒データ69
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ69(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "熟成ワイン")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "ヴィンテージワイン")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 10)
CASE "醸造数"
	LOCAL = 5 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 200 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
		IF TALENT:ARG:酒耐性 < 0
			LOCAL = 8
		ELSE
			LOCAL = 15
		ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ70
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ70(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "ヴィンテージワイン")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME,)
CASE "醸造数"
	LOCAL = 5 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 200 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF ABL:ARG:教養 >= 3
		LOCAL = 25
	ELSEIF TALENT:ARG:酒耐性 < 0
		LOCAL = 9
	ELSE
		LOCAL = 15
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT


@EXIST_酒データ71
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ71(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "ブランデーVS")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "ブランデーVSOP")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
CASE "醸造数"
	LOCAL = 3
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 900 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:酒耐性 < 1
		LOCAL = 1
	ELSEIF TALENT:ARG:酒耐性 < 2
		LOCAL = 6
	ELSEIF TALENT:ARG:酒耐性 < 3
		LOCAL = 18
	ELSE
		LOCAL = 22
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ72
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ72(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "ブランデーVSOP")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "ブランデーXO")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 10)
CASE "醸造数"
	LOCAL = 3
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 900 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:酒耐性 < 1
		LOCAL = 2
	ELSEIF TALENT:ARG:酒耐性 < 2
		LOCAL = 8
	ELSEIF TALENT:ARG:酒耐性 < 3
		LOCAL = 20
	ELSE
		LOCAL = 25
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ73
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ73(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "ブランデーXO")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
CASE "醸造数"
	LOCAL = 3
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 900 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF ABL:ARG:教養 >= 3
		LOCAL = 30
	ELSEIF TALENT:ARG:酒耐性 < 1
		LOCAL = 3
	ELSEIF TALENT:ARG:酒耐性 < 2
		LOCAL = 10
	ELSEIF TALENT:ARG:酒耐性 < 3
		LOCAL = 20
	ELSE
		LOCAL = 25
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT


@EXIST_酒データ74
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ74(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "灰持酒")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 0)
CASE "醸造数"
	LOCAL = 10
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 300 + RAND:400
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:神霊
		LOCAL = 20
	ELSEIF TALENT:ARG:酒耐性 < 0
		LOCAL = 10
	ELSEIF TALENT:ARG:酒耐性 < 2
		LOCAL = 15
	ELSE
		LOCAL = 8
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ75
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ75(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "ポマースブランデー")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
CASE "醸造数"
	LOCAL = 0
	CALLF MAKE_INT(V_NAME,)
CASE "酒気増加量"
	LOCAL = 900 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:酒耐性 < 1
		LOCAL = 1
	ELSEIF TALENT:ARG:酒耐性 < 2
		LOCAL = 6
	ELSEIF TALENT:ARG:酒耐性 < 3
		LOCAL = 18
	ELSE
		LOCAL = 22
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ76
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ76(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "もろみ(米)")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "にごり酒")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ77
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ77(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "にごり酒")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "吟醸酒")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 2)
CASE "醸造数"
	LOCAL = 5 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 400 + RAND:200
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
		IF TALENT:ARG:酒耐性 < 0
			LOCAL = 12
		ELSEIF TALENT:ARG:酒耐性 < 3
			LOCAL = 17
		ELSE
			LOCAL = 10
		ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ78
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ78(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "吟醸酒")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "熟成酒")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
CASE "醸造数"
	LOCAL = 4 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 500 + RAND:100
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
		IF TALENT:ARG:酒耐性 < 0
			LOCAL = 9
		ELSEIF TALENT:ARG:酒耐性 < 4
			LOCAL = 17
		ELSE
			LOCAL = 12
		ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ79
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ79(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "熟成酒")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "古酒")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 10)
CASE "醸造数"
	LOCAL = 3 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 450 + RAND:100
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
		IF TALENT:ARG:酒耐性 < 0
			LOCAL = 9
		ELSEIF TALENT:ARG:酒耐性 < 2
			LOCAL = 15
		ELSEIF TALENT:ARG:酒耐性 < 4
			LOCAL = 20
		ELSE
			LOCAL = 15
		ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ80
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ80(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "古酒")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME,)
CASE "醸造数"
	LOCAL = 2 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 400 + RAND:100
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
		IF TALENT:ARG:酒耐性 < 0
			LOCAL = 10
		ELSEIF TALENT:ARG:酒耐性 < 2
			LOCAL = 12
		ELSEIF TALENT:ARG:酒耐性 < 4
			LOCAL = 25
		ELSE
			LOCAL = 20
		ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ81
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ81(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "もろみ(再醸仕込)")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "貴醸にごり酒")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 7)
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
;CASE "酒気増加量"
;	CALLF MAKE_INT(V_NAME, )
;CASE "TASTE"
;	CALLF MAKE_INT(V_NAME, )
ENDSELECT

@EXIST_酒データ82
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ82(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "貴醸にごり酒")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "貴醸酒")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 2)
CASE "醸造数"
	LOCAL = 4 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 400 + RAND:300
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
		IF TALENT:ARG:酒耐性 < 0
			LOCAL = 17
		ELSEIF TALENT:ARG:酒耐性 < 3
			LOCAL = 25
		ELSE
			LOCAL = 8
		ENDIF
		SIF ABL:ARG:教養 >= 3
			LOCAL += 5
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ83
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ83(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "貴醸酒")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "貴醸熟成酒")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
CASE "醸造数"
	LOCAL = 3 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 500 + RAND:200
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
		IF TALENT:ARG:酒耐性 < 0
			LOCAL = 12
		ELSEIF TALENT:ARG:酒耐性 < 4
			LOCAL = 25
		ELSE
			LOCAL = 10
		ENDIF
		SIF ABL:ARG:教養 >= 3
			LOCAL += 5
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ84
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ84(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "貴醸熟成酒")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "貴醸古酒")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 10)
CASE "醸造数"
	LOCAL = 3 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 450 + RAND:200
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
		IF TALENT:ARG:酒耐性 < 0
			LOCAL = 12
		ELSEIF TALENT:ARG:酒耐性 < 1
			LOCAL = 20
		ELSEIF TALENT:ARG:酒耐性 < 4
			LOCAL = 25
		ELSE
			LOCAL = 15
		ENDIF
		SIF ABL:ARG:教養 >= 3
			LOCAL += 7
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ85
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ85(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "貴醸古酒")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME,)
CASE "醸造数"
	LOCAL = 2 + RAND:(ABL:MASTER:教養 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 400 + RAND:200
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
		IF TALENT:ARG:酒耐性 < 0
			LOCAL = 12
		ELSEIF TALENT:ARG:酒耐性 < 1
			LOCAL = 20
		ELSEIF TALENT:ARG:酒耐性 < 4
			LOCAL = 25
		ELSE
			LOCAL = 17
		ENDIF
		SIF ABL:ARG:教養 >= 3
			LOCAL += 10
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ86
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ86(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "粕取り焼酎")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
;CASE "醸造数"
;	CALLF MAKE_INT(V_NAME, )
CASE "酒気増加量"
	LOCAL = 700 + RAND:200
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
	IF TALENT:ARG:酒耐性 < 0
		LOCAL = 3
	ELSEIF TALENT:ARG:酒耐性 < 1
		LOCAL = 10
	ELSEIF TALENT:ARG:酒耐性 < 2
		LOCAL = 12
	ELSE
		LOCAL = 20
	ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

@EXIST_酒データ87
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データ87(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "名称"
	CALLF MAKE_STR(V_NAME, "三増酒")
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "")
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, )
CASE "醸造数"
	LOCAL = 30 + (RAND:(ABL:MASTER:教養 + 1) * 3)
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "酒気増加量"
	LOCAL = 300 + RAND:500
	CALLF MAKE_INT(V_NAME, LOCAL)
CASE "TASTE"
		IF TALENT:ARG:酒耐性 < 1
			LOCAL = 5
		ELSEIF TALENT:ARG:酒耐性 < 3
			LOCAL = 8
		ELSE
			LOCAL = 10
		ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT

;酒番号から名称を取得
@ALC_NAME(ARG)
#FUNCTIONS
#DIM CHARA
RETURNF GET_STR(0, "酒データ", ARG, "名称")

;名称から酒番号を取得
@ALC_ID(ARGS)
#FUNCTION
SIF ARGS == ""
	ARGS = %VAR_FERMENT%
FOR LOCAL,1,100
	SIF GET_STR(0, "酒データ", LOCAL, "名称") == ARGS
		RETURNF LOCAL
NEXT
RETURNF 0

@IS_ALC(ARG)
#FUNCTION
#DIM 酒ID
LOCALS = %ITEMNAME:ARG%
FOR 酒ID, 1, 100
	IF GET_STR(0, "酒データ", 酒ID, "名称") == LOCALS
		IF GET_INT(0, "酒データ", 酒ID, "酒気増加量") > 0
			RETURNF 1
		ELSE
			RETURNF 0
		ENDIF
	ENDIF
NEXT
RETURNF 0

;廃止
;@醸造日数(ARG)
;@醸造できた量(ARG)


@酒造
#DIMS ALTER
DAY:10 ++
LOCAL = GET_INT(0, "酒データ", ALC_ID(), "醸造日数")
IF LOCAL > 0 && LOCAL <= DAY:10
	ALTER = %GET_STR(0, "酒データ", ALC_ID(), "変化先")%
	CALL COLORMESSAGE(@"酒虫の瓶の中の%VAR_FERMENT%は%ALTER%に変化した",C_YELLOW, 2)
	DAY:10 = 0
	VAR_FERMENT = %ALTER%
ENDIF

@DRUNKEN(C_ID, itemnum)
#DIM C_ID
#DIM 酒ID
#DIM itemnum
#DIM 酒気増加量

LOCALS = %ITEMNAME:itemnum%

酒ID = ALC_ID(LOCALS)

酒気増加量 = GET_INT(C_ID, "酒データ", 酒ID, "酒気増加量")
;一夜のクシナダを除く
IF LOCALS != "一夜のクシナダ"
	IF TALENT:C_ID:酒耐性 == -1
		TIMES 酒気増加量, 1.2
	ELSEIF TALENT:C_ID:酒耐性 == 1
		TIMES 酒気増加量, 0.9
	ELSEIF TALENT:C_ID:酒耐性 == 2
		TIMES 酒気増加量, 0.75
	ELSEIF TALENT:C_ID:酒耐性 == 3
		TIMES 酒気増加量, 0.5
	ELSEIF TALENT:C_ID:酒耐性 == -2
		IF CFLAG:C_ID:下戸飲み >= 5
			TIMES 酒気増加量, 2.0
		ELSEIF CFLAG:C_ID:下戸飲み >= 2
			TIMES 酒気増加量, 1.5
		ELSE
			TIMES 酒気増加量, 1.0
		ENDIF
	ENDIF
ENDIF
SIF C_ID == MASTER
	酒気増加量 = 酒気増加量 / (1 + TFLAG:酔い止め)
BASE:C_ID:酒気 = MIN(BASE:C_ID:酒気 + 酒気増加量 ,MAXBASE:C_ID:酒気)

@酒一覧表示(ARG, ARGS, 割合)
;所持数がARG未満だと非表示、ARG以上所有してる酒を選択するとそのアイテム番号をRETURNする
#DIM ITEM_ID
#DIM 割合
$LOOP

FOR ITEM_ID, 0, 600
	IF IS_ALC(ITEM_ID) && ITEM:ITEM_ID >= ARG
		IF ARGS == "売却"
			PRINTFORML [{ITEM_ID}]%ITEMNAME:ITEM_ID, 20, LEFT% (%金額表示(ITEMPRICE:ITEM_ID * 割合 / 100), 7, RIGHT% / 所持: {ITEM:ITEM_ID, 3, RIGHT})
		ELSE
			CALL PRINT_ITEM_EXPLANATION(ITEM_ID)
		ENDIF
	ENDIF
NEXT
DRAWLINE 
PRINTL [999] キャンセル
INPUT
IF IS_ALC(RESULT) && ITEM:RESULT >= ARG
	RETURN RESULT
ELSEIF RESULT == 999
	RETURN 0
ELSE
	GOTO LOOP
ENDIF

@CAN_DRINK(ARG)
#FUNCTION
SIF TALENT:ARG:酒耐性 == -2
	RETURNF 0
SIF BASE:ARG:酒気 >= MAXBASE:ARG:酒気 * 2 / 3
	RETURNF 0
RETURNF 1

@ALCOHOL_TASTE(ARG)
#FUNCTION
#DIM 酒ID
LOCALS = %ITEMNAME:ARG%
FOR 酒ID, 1, 100
	IF GET_STR(0, "酒データ", 酒ID, "名称") == LOCALS
		LOCAL = GET_INT(TARGET, "酒データ", 酒ID, "TASTE")
		LOCAL += TFLAG:口上特殊補正
		RETURNF LOCAL
	ENDIF
NEXT
RETURNF 0

[SKIPSTART]
;テンプレ
;EXIST関数忘れると動きません
@EXIST_酒データX
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@酒データX(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
;名前、水でも酒でもこれがないと始まらない
CASE "名称"
	CALLF MAKE_STR(V_NAME, "どぶろく")
;瓶で放置してたら何に変化するか
;変化しないなら未記載
CASE "変化先"
	CALLF MAKE_STR(V_NAME, "清酒")
;変化に必要な日数、しないなら未記載
CASE "醸造日数"
	CALLF MAKE_INT(V_NAME, 5)
;汲みだすときに手に入る数
;水とか調合で入手するやつは未記載
CASE "醸造数"
	LOCAL = 10 + RAND:(ABL:MASTER:教養 * 2 + 1)
	CALLF MAKE_INT(V_NAME, LOCAL)
;酒飲んだ時の酒気増加量
;後で行われるので耐性による補正は無視してよい
;ここが未記入でないなら、当該名称のアイテムは酒扱いされる
CASE "酒気増加量"
	LOCAL = 300 + RAND:100
	CALLF MAKE_INT(V_NAME, LOCAL)
;反応、10が基本値
CASE "TASTE"
		IF TALENT:ARG:酒耐性 > 0
			LOCAL = 7
		ELSE
			LOCAL = 10
		ENDIF
	CALLF MAKE_INT(V_NAME, LOCAL)
ENDSELECT
[SKIPEND]